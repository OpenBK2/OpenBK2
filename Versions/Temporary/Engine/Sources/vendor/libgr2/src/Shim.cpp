// Generated by scripts/port/gen-granny-shim.py. Do not edit.
//
// A granny2 that records and forwards. Every entry point logs its arguments the
// way libgr2's own does, in the same format and with the same stable object ids,
// and then calls the real DLL. Two runs of the same play, one against each, give
// two logs that diff.
//
// Built as the `gr2-shim` target, which is Windows only and always defines
// LIBGR2_TRACE_CALLS: a shim with the trace compiled out would do nothing at all
// but slow the game down. See ShimLoader.h for which DLL it forwards to.
//
// The trace lines here are `Parameter={}` for each parameter in declaration
// order, which is the convention every hand-written GR2_TRACE in this library
// follows. That is what makes the two logs line up.

#include <gr2/granny.h>

#if defined( _WIN32 )

#include "Identify.h"
#include "ShimLoader.h"
#include "Structures.h"
#include "Trace.h"

#include <cstddef>

extern "C"
{

GR2_API( granny_controlled_animation_builder * ) GrannyBeginControlledAnimation( granny_real32 StartTime, granny_animation const *Animation )
{
	GR2_TRACE( "StartTime={} Animation={}", StartTime, Animation );

	typedef granny_controlled_animation_builder *( __stdcall *TFn )( granny_real32 StartTime, granny_animation const *Animation );
	typedef granny_controlled_animation_builder * TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyBeginControlledAnimation" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( StartTime, Animation );
	return Result;
}

GR2_API( void ) GrannyBuildWorldPose( granny_skeleton const *Skeleton, granny_int32x FirstBone, granny_int32x BoneCount, granny_local_pose const *LocalPose, granny_real32 const *Offset4x4, granny_world_pose *Result )
{
	GR2_TRACE( "Skeleton={} FirstBone={} BoneCount={} LocalPose={} Offset4x4={} Result={}", Skeleton, FirstBone, BoneCount, LocalPose, Offset4x4, Result );

	typedef void ( __stdcall *TFn )( granny_skeleton const *Skeleton, granny_int32x FirstBone, granny_int32x BoneCount, granny_local_pose const *LocalPose, granny_real32 const *Offset4x4, granny_world_pose *Result );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyBuildWorldPose" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Skeleton, FirstBone, BoneCount, LocalPose, Offset4x4, Result );
}

GR2_API( void ) GrannyCompleteControlAt( granny_control *Control, granny_real32 AtSeconds )
{
	GR2_TRACE( "Control={} AtSeconds={}", Control, AtSeconds );

	typedef void ( __stdcall *TFn )( granny_control *Control, granny_real32 AtSeconds );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyCompleteControlAt" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Control, AtSeconds );
}

GR2_API( bool ) GrannyControlIsComplete( granny_control const *Control )
{
	GR2_TRACE( "Control={}", Control );

	typedef bool ( __stdcall *TFn )( granny_control const *Control );
	typedef bool TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyControlIsComplete" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Control );
	return Result;
}

GR2_API( void ) GrannyConvertSingleObject( granny_data_type_definition const *SourceType, void const *SourceObject, granny_data_type_definition const *DestType, void *DestObject, granny_conversion_handler *OverrideHandler )
{
	GR2_TRACE( "SourceType={} SourceObject={} DestType={} DestObject={} OverrideHandler={}", SourceType, SourceObject, DestType, DestObject, OverrideHandler );

	typedef void ( __stdcall *TFn )( granny_data_type_definition const *SourceType, void const *SourceObject, granny_data_type_definition const *DestType, void *DestObject, granny_conversion_handler *OverrideHandler );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyConvertSingleObject" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( SourceType, SourceObject, DestType, DestObject, OverrideHandler );
}

GR2_API( granny_real32 ) GrannyEaseControlIn( granny_control *Control, granny_real32 Duration, bool FromCurrent )
{
	GR2_TRACE( "Control={} Duration={} FromCurrent={}", Control, Duration, FromCurrent );

	typedef granny_real32 ( __stdcall *TFn )( granny_control *Control, granny_real32 Duration, bool FromCurrent );
	typedef granny_real32 TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyEaseControlIn" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Control, Duration, FromCurrent );
	return Result;
}

GR2_API( granny_real32 ) GrannyEaseControlOut( granny_control *Control, granny_real32 Duration )
{
	GR2_TRACE( "Control={} Duration={}", Control, Duration );

	typedef granny_real32 ( __stdcall *TFn )( granny_control *Control, granny_real32 Duration );
	typedef granny_real32 TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyEaseControlOut" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Control, Duration );
	return Result;
}

GR2_API( granny_control * ) GrannyEndControlledAnimation( granny_controlled_animation_builder *Builder )
{
	GR2_TRACE( "Builder={}", Builder );

	typedef granny_control *( __stdcall *TFn )( granny_controlled_animation_builder *Builder );
	typedef granny_control * TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyEndControlledAnimation" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	NGr2::RetireHandle( Builder );

	TResult Result = pfn( Builder );
	return Result;
}

GR2_API( void ) GrannyEvaluateCurveAtT( granny_int32x Dimension, bool Normalize, bool BackwardsLoop, granny_curve2 const *Curve, bool ForwardsLoop, granny_real32 CurveDuration, granny_real32 t, granny_real32 *Result, granny_real32 const *IdentityVector )
{
	GR2_TRACE( "Dimension={} Normalize={} BackwardsLoop={} Curve={} ForwardsLoop={} CurveDuration={} t={} Result={} IdentityVector={}", Dimension, Normalize, BackwardsLoop, Curve, ForwardsLoop, CurveDuration, t, Result, IdentityVector );

	typedef void ( __stdcall *TFn )( granny_int32x Dimension, bool Normalize, bool BackwardsLoop, granny_curve2 const *Curve, bool ForwardsLoop, granny_real32 CurveDuration, granny_real32 t, granny_real32 *Result, granny_real32 const *IdentityVector );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyEvaluateCurveAtT" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Dimension, Normalize, BackwardsLoop, Curve, ForwardsLoop, CurveDuration, t, Result, IdentityVector );
}

GR2_API( bool ) GrannyFindBoneByName( granny_skeleton const *Skeleton, char const *BoneName, granny_int32x *BoneIndex )
{
	GR2_TRACE( "Skeleton={} BoneName={} BoneIndex={}", Skeleton, BoneName, BoneIndex );

	typedef bool ( __stdcall *TFn )( granny_skeleton const *Skeleton, char const *BoneName, granny_int32x *BoneIndex );
	typedef bool TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyFindBoneByName" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Skeleton, BoneName, BoneIndex );
	return Result;
}

GR2_API( void ) GrannyFreeControl( granny_control *Control )
{
	GR2_TRACE( "Control={}", Control );

	typedef void ( __stdcall *TFn )( granny_control *Control );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyFreeControl" ) );
	if ( pfn == 0 )
	{
		return;
	}
	NGr2::RetireHandle( Control );

	pfn( Control );
}

GR2_API( void ) GrannyFreeControlOnceUnused( granny_control *Control )
{
	GR2_TRACE( "Control={}", Control );

	typedef void ( __stdcall *TFn )( granny_control *Control );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyFreeControlOnceUnused" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Control );
}

GR2_API( void ) GrannyFreeFile( granny_file *File )
{
	GR2_TRACE( "File={}", File );

	typedef void ( __stdcall *TFn )( granny_file *File );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyFreeFile" ) );
	if ( pfn == 0 )
	{
		return;
	}
	NGr2::ForgetFile( File );
	NGr2::RetireHandle( File );

	pfn( File );
}

GR2_API( void ) GrannyFreeLocalPose( granny_local_pose *LocalPose )
{
	GR2_TRACE( "LocalPose={}", LocalPose );

	typedef void ( __stdcall *TFn )( granny_local_pose *LocalPose );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyFreeLocalPose" ) );
	if ( pfn == 0 )
	{
		return;
	}
	NGr2::RetireHandle( LocalPose );

	pfn( LocalPose );
}

GR2_API( void ) GrannyFreeModelInstance( granny_model_instance *ModelInstance )
{
	GR2_TRACE( "ModelInstance={}", ModelInstance );

	typedef void ( __stdcall *TFn )( granny_model_instance *ModelInstance );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyFreeModelInstance" ) );
	if ( pfn == 0 )
	{
		return;
	}
	NGr2::RetireHandle( ModelInstance );

	pfn( ModelInstance );
}

GR2_API( void ) GrannyFreeWorldPose( granny_world_pose *WorldPose )
{
	GR2_TRACE( "WorldPose={}", WorldPose );

	typedef void ( __stdcall *TFn )( granny_world_pose *WorldPose );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyFreeWorldPose" ) );
	if ( pfn == 0 )
	{
		return;
	}
	NGr2::RetireHandle( WorldPose );

	pfn( WorldPose );
}

GR2_API( void ) GrannyGetAllocator( granny_allocate_callback **AllocateCallback, granny_deallocate_callback **DeallocateCallback )
{
	GR2_TRACE( "AllocateCallback={} DeallocateCallback={}", AllocateCallback, DeallocateCallback );

	typedef void ( __stdcall *TFn )( granny_allocate_callback **AllocateCallback, granny_deallocate_callback **DeallocateCallback );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetAllocator" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( AllocateCallback, DeallocateCallback );
}

GR2_API( granny_real32 ) GrannyGetControlClampedLocalClock( granny_control *Control )
{
	GR2_TRACE( "Control={}", Control );

	typedef granny_real32 ( __stdcall *TFn )( granny_control *Control );
	typedef granny_real32 TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetControlClampedLocalClock" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Control );
	return Result;
}

GR2_API( granny_real32 ) GrannyGetControlDuration( granny_control const *Control )
{
	GR2_TRACE( "Control={}", Control );

	typedef granny_real32 ( __stdcall *TFn )( granny_control const *Control );
	typedef granny_real32 TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetControlDuration" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Control );
	return Result;
}

GR2_API( granny_real32 ) GrannyGetControlDurationLeft( granny_control *Control )
{
	GR2_TRACE( "Control={}", Control );

	typedef granny_real32 ( __stdcall *TFn )( granny_control *Control );
	typedef granny_real32 TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetControlDurationLeft" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Control );
	return Result;
}

GR2_API( granny_real32 ) GrannyGetControlEffectiveWeight( granny_control const *Control )
{
	GR2_TRACE( "Control={}", Control );

	typedef granny_real32 ( __stdcall *TFn )( granny_control const *Control );
	typedef granny_real32 TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetControlEffectiveWeight" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Control );
	return Result;
}

GR2_API( granny_real32 ) GrannyGetControlSpeed( granny_control const *Control )
{
	GR2_TRACE( "Control={}", Control );

	typedef granny_real32 ( __stdcall *TFn )( granny_control const *Control );
	typedef granny_real32 TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetControlSpeed" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Control );
	return Result;
}

GR2_API( granny_file_info * ) GrannyGetFileInfo( granny_file *File )
{
	GR2_TRACE( "File={}", File );

	typedef granny_file_info *( __stdcall *TFn )( granny_file *File );
	typedef granny_file_info * TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetFileInfo" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( File );

	NGr2::NoteFileInfo( File, reinterpret_cast<const NGr2::SFileInfo *>( Result ) );
	return Result;
}

GR2_API( granny_int32x ) GrannyGetLocalPoseBoneCount( granny_local_pose const *LocalPose )
{
	GR2_TRACE( "LocalPose={}", LocalPose );

	typedef granny_int32x ( __stdcall *TFn )( granny_local_pose const *LocalPose );
	typedef granny_int32x TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetLocalPoseBoneCount" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( LocalPose );
	return Result;
}

GR2_API( granny_transform * ) GrannyGetLocalPoseTransform( granny_local_pose const *LocalPose, granny_int32x BoneIndex )
{
	GR2_TRACE( "LocalPose={} BoneIndex={}", LocalPose, BoneIndex );

	typedef granny_transform *( __stdcall *TFn )( granny_local_pose const *LocalPose, granny_int32x BoneIndex );
	typedef granny_transform * TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetLocalPoseTransform" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( LocalPose, BoneIndex );
	return Result;
}

GR2_API( granny_int32x ) GrannyGetMemberTypeSize( granny_data_type_definition const *MemberType )
{
	GR2_TRACE( "MemberType={}", MemberType );

	typedef granny_int32x ( __stdcall *TFn )( granny_data_type_definition const *MemberType );
	typedef granny_int32x TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetMemberTypeSize" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( MemberType );
	return Result;
}

GR2_API( granny_int32x ) GrannyGetMeshBytesPerIndex( granny_mesh const *Mesh )
{
	GR2_TRACE( "Mesh={}", Mesh );

	typedef granny_int32x ( __stdcall *TFn )( granny_mesh const *Mesh );
	typedef granny_int32x TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetMeshBytesPerIndex" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Mesh );
	return Result;
}

GR2_API( granny_int32x ) GrannyGetMeshIndexCount( granny_mesh const *Mesh )
{
	GR2_TRACE( "Mesh={}", Mesh );

	typedef granny_int32x ( __stdcall *TFn )( granny_mesh const *Mesh );
	typedef granny_int32x TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetMeshIndexCount" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Mesh );
	return Result;
}

GR2_API( void * ) GrannyGetMeshIndices( granny_mesh const *Mesh )
{
	GR2_TRACE( "Mesh={}", Mesh );

	typedef void *( __stdcall *TFn )( granny_mesh const *Mesh );
	typedef void * TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetMeshIndices" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Mesh );
	return Result;
}

GR2_API( granny_int32x ) GrannyGetMeshTriangleGroupCount( granny_mesh const *Mesh )
{
	GR2_TRACE( "Mesh={}", Mesh );

	typedef granny_int32x ( __stdcall *TFn )( granny_mesh const *Mesh );
	typedef granny_int32x TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetMeshTriangleGroupCount" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Mesh );
	return Result;
}

GR2_API( granny_int32x ) GrannyGetMeshVertexCount( granny_mesh const *Mesh )
{
	GR2_TRACE( "Mesh={}", Mesh );

	typedef granny_int32x ( __stdcall *TFn )( granny_mesh const *Mesh );
	typedef granny_int32x TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetMeshVertexCount" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Mesh );
	return Result;
}

GR2_API( granny_data_type_definition * ) GrannyGetMeshVertexType( granny_mesh const *Mesh )
{
	GR2_TRACE( "Mesh={}", Mesh );

	typedef granny_data_type_definition *( __stdcall *TFn )( granny_mesh const *Mesh );
	typedef granny_data_type_definition * TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetMeshVertexType" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Mesh );
	return Result;
}

GR2_API( void * ) GrannyGetMeshVertices( granny_mesh const *Mesh )
{
	GR2_TRACE( "Mesh={}", Mesh );

	typedef void *( __stdcall *TFn )( granny_mesh const *Mesh );
	typedef void * TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetMeshVertices" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Mesh );
	return Result;
}

GR2_API( granny_int32x ) GrannyGetTotalObjectSize( granny_data_type_definition const *TypeDefinition )
{
	GR2_TRACE( "TypeDefinition={}", TypeDefinition );

	typedef granny_int32x ( __stdcall *TFn )( granny_data_type_definition const *TypeDefinition );
	typedef granny_int32x TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetTotalObjectSize" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( TypeDefinition );
	return Result;
}

GR2_API( granny_real32 * ) GrannyGetWorldPose4x4( granny_world_pose const *WorldPose, granny_int32x BoneIndex )
{
	GR2_TRACE( "WorldPose={} BoneIndex={}", WorldPose, BoneIndex );

	typedef granny_real32 *( __stdcall *TFn )( granny_world_pose const *WorldPose, granny_int32x BoneIndex );
	typedef granny_real32 * TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetWorldPose4x4" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( WorldPose, BoneIndex );
	return Result;
}

GR2_API( granny_real32 * ) GrannyGetWorldPoseComposite4x4( granny_world_pose const *WorldPose, granny_int32x BoneIndex )
{
	GR2_TRACE( "WorldPose={} BoneIndex={}", WorldPose, BoneIndex );

	typedef granny_real32 *( __stdcall *TFn )( granny_world_pose const *WorldPose, granny_int32x BoneIndex );
	typedef granny_real32 * TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyGetWorldPoseComposite4x4" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( WorldPose, BoneIndex );
	return Result;
}

GR2_API( granny_model_instance * ) GrannyInstantiateModel( granny_model const *Model )
{
	GR2_TRACE( "Model={}", Model );

	typedef granny_model_instance *( __stdcall *TFn )( granny_model const *Model );
	typedef granny_model_instance * TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyInstantiateModel" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Model );
	return Result;
}

GR2_API( void ) GrannyMakeIdentity( granny_transform *Result )
{
	GR2_TRACE( "Result={}", Result );

	typedef void ( __stdcall *TFn )( granny_transform *Result );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyMakeIdentity" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Result );
}

GR2_API( bool ) GrannyMeshIsRigid( granny_mesh const *Mesh )
{
	GR2_TRACE( "Mesh={}", Mesh );

	typedef bool ( __stdcall *TFn )( granny_mesh const *Mesh );
	typedef bool TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyMeshIsRigid" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( Mesh );
	return Result;
}

GR2_API( granny_local_pose * ) GrannyNewLocalPose( granny_int32x BoneCount )
{
	GR2_TRACE( "BoneCount={}", BoneCount );

	typedef granny_local_pose *( __stdcall *TFn )( granny_int32x BoneCount );
	typedef granny_local_pose * TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyNewLocalPose" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( BoneCount );
	return Result;
}

GR2_API( granny_track_mask * ) GrannyNewTrackMask( granny_real32 DefaultWeight, granny_int32x BoneCount )
{
	GR2_TRACE( "DefaultWeight={} BoneCount={}", DefaultWeight, BoneCount );

	typedef granny_track_mask *( __stdcall *TFn )( granny_real32 DefaultWeight, granny_int32x BoneCount );
	typedef granny_track_mask * TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyNewTrackMask" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( DefaultWeight, BoneCount );
	return Result;
}

GR2_API( granny_world_pose * ) GrannyNewWorldPose( granny_int32x BoneCount )
{
	GR2_TRACE( "BoneCount={}", BoneCount );

	typedef granny_world_pose *( __stdcall *TFn )( granny_int32x BoneCount );
	typedef granny_world_pose * TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyNewWorldPose" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( BoneCount );
	return Result;
}

GR2_API( void ) GrannyPostMultiplyBy( granny_transform *Transform, granny_transform const *PostMult )
{
	GR2_TRACE( "Transform={} PostMult={}", Transform, PostMult );

	typedef void ( __stdcall *TFn )( granny_transform *Transform, granny_transform const *PostMult );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyPostMultiplyBy" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Transform, PostMult );
}

GR2_API( granny_file * ) GrannyReadEntireFile( char const *FileName )
{
	GR2_TRACE( "FileName={}", FileName );

	typedef granny_file *( __stdcall *TFn )( char const *FileName );
	typedef granny_file * TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyReadEntireFile" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( FileName );

	NGr2::NoteFileName( Result, FileName );
	return Result;
}

GR2_API( granny_file * ) GrannyReadEntireFileFromMemory( granny_int32x MemorySize, void const *Memory )
{
	GR2_TRACE( "MemorySize={} Memory={}", MemorySize, Memory );

	typedef granny_file *( __stdcall *TFn )( granny_int32x MemorySize, void const *Memory );
	typedef granny_file * TResult;
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannyReadEntireFileFromMemory" ) );
	if ( pfn == 0 )
	{
		return TResult();
	}
	TResult Result = pfn( MemorySize, Memory );

	NGr2::NoteFileBytes( Result, Memory,
	                      MemorySize > 0 ? static_cast<size_t>( MemorySize ) : 0u );
	return Result;
}

GR2_API( void ) GrannySampleModelAnimations( granny_model_instance const *ModelInstance, granny_int32x FirstBone, granny_int32x BoneCount, granny_local_pose *Result )
{
	GR2_TRACE( "ModelInstance={} FirstBone={} BoneCount={} Result={}", ModelInstance, FirstBone, BoneCount, Result );

	typedef void ( __stdcall *TFn )( granny_model_instance const *ModelInstance, granny_int32x FirstBone, granny_int32x BoneCount, granny_local_pose *Result );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannySampleModelAnimations" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( ModelInstance, FirstBone, BoneCount, Result );
}

GR2_API( void ) GrannySetAllocator( granny_allocate_callback *AllocateCallback, granny_deallocate_callback *DeallocateCallback )
{
	GR2_TRACE( "AllocateCallback={} DeallocateCallback={}", AllocateCallback, DeallocateCallback );

	typedef void ( __stdcall *TFn )( granny_allocate_callback *AllocateCallback, granny_deallocate_callback *DeallocateCallback );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannySetAllocator" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( AllocateCallback, DeallocateCallback );
}

GR2_API( void ) GrannySetControlActive( granny_control *Control, bool Active )
{
	GR2_TRACE( "Control={} Active={}", Control, Active );

	typedef void ( __stdcall *TFn )( granny_control *Control, bool Active );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannySetControlActive" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Control, Active );
}

GR2_API( void ) GrannySetControlEaseIn( granny_control *Control, bool EaseIn )
{
	GR2_TRACE( "Control={} EaseIn={}", Control, EaseIn );

	typedef void ( __stdcall *TFn )( granny_control *Control, bool EaseIn );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannySetControlEaseIn" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Control, EaseIn );
}

GR2_API( void ) GrannySetControlEaseInCurve( granny_control *Control, granny_real32 StartSeconds, granny_real32 EndSeconds, granny_real32 StartValue, granny_real32 StartTangent, granny_real32 EndTangent, granny_real32 EndValue )
{
	GR2_TRACE( "Control={} StartSeconds={} EndSeconds={} StartValue={} StartTangent={} EndTangent={} EndValue={}", Control, StartSeconds, EndSeconds, StartValue, StartTangent, EndTangent, EndValue );

	typedef void ( __stdcall *TFn )( granny_control *Control, granny_real32 StartSeconds, granny_real32 EndSeconds, granny_real32 StartValue, granny_real32 StartTangent, granny_real32 EndTangent, granny_real32 EndValue );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannySetControlEaseInCurve" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Control, StartSeconds, EndSeconds, StartValue, StartTangent, EndTangent, EndValue );
}

GR2_API( void ) GrannySetControlEaseOut( granny_control *Control, bool EaseOut )
{
	GR2_TRACE( "Control={} EaseOut={}", Control, EaseOut );

	typedef void ( __stdcall *TFn )( granny_control *Control, bool EaseOut );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannySetControlEaseOut" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Control, EaseOut );
}

GR2_API( void ) GrannySetControlEaseOutCurve( granny_control *Control, granny_real32 StartSeconds, granny_real32 EndSeconds, granny_real32 StartValue, granny_real32 StartTangent, granny_real32 EndTangent, granny_real32 EndValue )
{
	GR2_TRACE( "Control={} StartSeconds={} EndSeconds={} StartValue={} StartTangent={} EndTangent={} EndValue={}", Control, StartSeconds, EndSeconds, StartValue, StartTangent, EndTangent, EndValue );

	typedef void ( __stdcall *TFn )( granny_control *Control, granny_real32 StartSeconds, granny_real32 EndSeconds, granny_real32 StartValue, granny_real32 StartTangent, granny_real32 EndTangent, granny_real32 EndValue );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannySetControlEaseOutCurve" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Control, StartSeconds, EndSeconds, StartValue, StartTangent, EndTangent, EndValue );
}

GR2_API( void ) GrannySetControlForceClampedLooping( granny_control *Control, bool Clamp )
{
	GR2_TRACE( "Control={} Clamp={}", Control, Clamp );

	typedef void ( __stdcall *TFn )( granny_control *Control, bool Clamp );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannySetControlForceClampedLooping" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Control, Clamp );
}

GR2_API( void ) GrannySetControlLoopCount( granny_control *Control, granny_int32x LoopCount )
{
	GR2_TRACE( "Control={} LoopCount={}", Control, LoopCount );

	typedef void ( __stdcall *TFn )( granny_control *Control, granny_int32x LoopCount );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannySetControlLoopCount" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Control, LoopCount );
}

GR2_API( void ) GrannySetControlRawLocalClock( granny_control *Control, granny_real32 LocalClock )
{
	GR2_TRACE( "Control={} LocalClock={}", Control, LocalClock );

	typedef void ( __stdcall *TFn )( granny_control *Control, granny_real32 LocalClock );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannySetControlRawLocalClock" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Control, LocalClock );
}

GR2_API( void ) GrannySetControlSpeed( granny_control *Control, granny_real32 Speed )
{
	GR2_TRACE( "Control={} Speed={}", Control, Speed );

	typedef void ( __stdcall *TFn )( granny_control *Control, granny_real32 Speed );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannySetControlSpeed" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Control, Speed );
}

GR2_API( void ) GrannySetModelClock( granny_model_instance const *ModelInstance, granny_real32 NewClock )
{
	GR2_TRACE( "ModelInstance={} NewClock={}", ModelInstance, NewClock );

	typedef void ( __stdcall *TFn )( granny_model_instance const *ModelInstance, granny_real32 NewClock );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannySetModelClock" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( ModelInstance, NewClock );
}

GR2_API( void ) GrannySetSkeletonTrackMaskFromTrackGroup( granny_track_mask *Mask, granny_skeleton const *Skeleton, granny_track_group const *TrackGroup, granny_real32 IdentityValue, granny_real32 ConstantValue, granny_real32 AnimatedValue )
{
	GR2_TRACE( "Mask={} Skeleton={} TrackGroup={} IdentityValue={} ConstantValue={} AnimatedValue={}", Mask, Skeleton, TrackGroup, IdentityValue, ConstantValue, AnimatedValue );

	typedef void ( __stdcall *TFn )( granny_track_mask *Mask, granny_skeleton const *Skeleton, granny_track_group const *TrackGroup, granny_real32 IdentityValue, granny_real32 ConstantValue, granny_real32 AnimatedValue );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannySetSkeletonTrackMaskFromTrackGroup" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Mask, Skeleton, TrackGroup, IdentityValue, ConstantValue, AnimatedValue );
}

GR2_API( void ) GrannySetTrackGroupAccumulation( granny_controlled_animation_builder *Builder, granny_int32x TrackGroupIndex, granny_accumulation_mode Mode )
{
	GR2_TRACE( "Builder={} TrackGroupIndex={} Mode={}", Builder, TrackGroupIndex, Mode );

	typedef void ( __stdcall *TFn )( granny_controlled_animation_builder *Builder, granny_int32x TrackGroupIndex, granny_accumulation_mode Mode );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannySetTrackGroupAccumulation" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Builder, TrackGroupIndex, Mode );
}

GR2_API( void ) GrannySetTrackGroupModelMask( granny_controlled_animation_builder *Builder, granny_int32x TrackGroupIndex, granny_track_mask *ModelMask )
{
	GR2_TRACE( "Builder={} TrackGroupIndex={} ModelMask={}", Builder, TrackGroupIndex, ModelMask );

	typedef void ( __stdcall *TFn )( granny_controlled_animation_builder *Builder, granny_int32x TrackGroupIndex, granny_track_mask *ModelMask );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannySetTrackGroupModelMask" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Builder, TrackGroupIndex, ModelMask );
}

GR2_API( void ) GrannySetTrackGroupTarget( granny_controlled_animation_builder *Builder, granny_int32x TrackGroupIndex, granny_model_instance *Model )
{
	GR2_TRACE( "Builder={} TrackGroupIndex={} Model={}", Builder, TrackGroupIndex, Model );

	typedef void ( __stdcall *TFn )( granny_controlled_animation_builder *Builder, granny_int32x TrackGroupIndex, granny_model_instance *Model );
	static const TFn pfn = reinterpret_cast<TFn>( NGr2::ShimEntry( "GrannySetTrackGroupTarget" ) );
	if ( pfn == 0 )
	{
		return;
	}
	pfn( Builder, TrackGroupIndex, Model );
}

}

#endif
