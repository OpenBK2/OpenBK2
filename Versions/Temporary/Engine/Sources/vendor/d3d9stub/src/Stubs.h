// Generated by scripts/port/gen-d3d9-stub.py from the DirectX SDK's d3d9.h.
// Do not edit by hand: override in the hand-written classes instead.
//
// One class per interface, named C<interface without the I>Stub, with a real
// IUnknown and every other method recorded and failed. See the generator for
// why, and Trace.h for what D3D9_STUB records.

#pragma once

#include "Trace.h"

#include <d3d9.h>

#include <atomic>

namespace ND3D9Stub
{

class CDirect3D9Stub : public IDirect3D9
{
public:
	virtual ~CDirect3D9Stub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3D9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3D9 *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3D9", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(RegisterSoftwareDevice)( void* pInitializeFunction ) override
	{
		D3D9_STUB( "IDirect3D9", pInitializeFunction );
		return E_FAIL;
	}
	STDMETHOD_(UINT, GetAdapterCount)() override
	{
		D3D9_STUB0( "IDirect3D9" );
		return {};
	}
	STDMETHOD(GetAdapterIdentifier)( UINT Adapter, DWORD Flags, D3DADAPTER_IDENTIFIER9* pIdentifier ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, Flags, pIdentifier );
		return E_FAIL;
	}
	STDMETHOD_(UINT, GetAdapterModeCount)( UINT Adapter, D3DFORMAT Format ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, Format );
		return {};
	}
	STDMETHOD(EnumAdapterModes)( UINT Adapter, D3DFORMAT Format, UINT Mode, D3DDISPLAYMODE* pMode ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, Format, Mode, pMode );
		return E_FAIL;
	}
	STDMETHOD(GetAdapterDisplayMode)( UINT Adapter, D3DDISPLAYMODE* pMode ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, pMode );
		return E_FAIL;
	}
	STDMETHOD(CheckDeviceType)( UINT Adapter, D3DDEVTYPE DevType, D3DFORMAT AdapterFormat, D3DFORMAT BackBufferFormat, BOOL bWindowed ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, DevType, AdapterFormat, BackBufferFormat, bWindowed );
		return E_FAIL;
	}
	STDMETHOD(CheckDeviceFormat)( UINT Adapter, D3DDEVTYPE DeviceType, D3DFORMAT AdapterFormat, DWORD Usage, D3DRESOURCETYPE RType, D3DFORMAT CheckFormat ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, DeviceType, AdapterFormat, Usage, RType, CheckFormat );
		return E_FAIL;
	}
	STDMETHOD(CheckDeviceMultiSampleType)( UINT Adapter, D3DDEVTYPE DeviceType, D3DFORMAT SurfaceFormat, BOOL Windowed, D3DMULTISAMPLE_TYPE MultiSampleType, DWORD* pQualityLevels ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, DeviceType, SurfaceFormat, Windowed, MultiSampleType, pQualityLevels );
		return E_FAIL;
	}
	STDMETHOD(CheckDepthStencilMatch)( UINT Adapter, D3DDEVTYPE DeviceType, D3DFORMAT AdapterFormat, D3DFORMAT RenderTargetFormat, D3DFORMAT DepthStencilFormat ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, DeviceType, AdapterFormat, RenderTargetFormat, DepthStencilFormat );
		return E_FAIL;
	}
	STDMETHOD(CheckDeviceFormatConversion)( UINT Adapter, D3DDEVTYPE DeviceType, D3DFORMAT SourceFormat, D3DFORMAT TargetFormat ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, DeviceType, SourceFormat, TargetFormat );
		return E_FAIL;
	}
	STDMETHOD(GetDeviceCaps)( UINT Adapter, D3DDEVTYPE DeviceType, D3DCAPS9* pCaps ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, DeviceType, pCaps );
		return E_FAIL;
	}
	STDMETHOD_(HMONITOR, GetAdapterMonitor)( UINT Adapter ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter );
		return {};
	}
	STDMETHOD(CreateDevice)( UINT Adapter, D3DDEVTYPE DeviceType, HWND hFocusWindow, DWORD BehaviorFlags, D3DPRESENT_PARAMETERS* pPresentationParameters, IDirect3DDevice9** ppReturnedDeviceInterface ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, DeviceType, hFocusWindow, BehaviorFlags, pPresentationParameters, ppReturnedDeviceInterface );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3D9ExStub : public IDirect3D9Ex
{
public:
	virtual ~CDirect3D9ExStub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3D9Ex || riid == IID_IDirect3D9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3D9Ex *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3D9Ex", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(RegisterSoftwareDevice)( void* pInitializeFunction ) override
	{
		D3D9_STUB( "IDirect3D9", pInitializeFunction );
		return E_FAIL;
	}
	STDMETHOD_(UINT, GetAdapterCount)() override
	{
		D3D9_STUB0( "IDirect3D9" );
		return {};
	}
	STDMETHOD(GetAdapterIdentifier)( UINT Adapter, DWORD Flags, D3DADAPTER_IDENTIFIER9* pIdentifier ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, Flags, pIdentifier );
		return E_FAIL;
	}
	STDMETHOD_(UINT, GetAdapterModeCount)( UINT Adapter, D3DFORMAT Format ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, Format );
		return {};
	}
	STDMETHOD(EnumAdapterModes)( UINT Adapter, D3DFORMAT Format, UINT Mode, D3DDISPLAYMODE* pMode ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, Format, Mode, pMode );
		return E_FAIL;
	}
	STDMETHOD(GetAdapterDisplayMode)( UINT Adapter, D3DDISPLAYMODE* pMode ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, pMode );
		return E_FAIL;
	}
	STDMETHOD(CheckDeviceType)( UINT Adapter, D3DDEVTYPE DevType, D3DFORMAT AdapterFormat, D3DFORMAT BackBufferFormat, BOOL bWindowed ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, DevType, AdapterFormat, BackBufferFormat, bWindowed );
		return E_FAIL;
	}
	STDMETHOD(CheckDeviceFormat)( UINT Adapter, D3DDEVTYPE DeviceType, D3DFORMAT AdapterFormat, DWORD Usage, D3DRESOURCETYPE RType, D3DFORMAT CheckFormat ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, DeviceType, AdapterFormat, Usage, RType, CheckFormat );
		return E_FAIL;
	}
	STDMETHOD(CheckDeviceMultiSampleType)( UINT Adapter, D3DDEVTYPE DeviceType, D3DFORMAT SurfaceFormat, BOOL Windowed, D3DMULTISAMPLE_TYPE MultiSampleType, DWORD* pQualityLevels ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, DeviceType, SurfaceFormat, Windowed, MultiSampleType, pQualityLevels );
		return E_FAIL;
	}
	STDMETHOD(CheckDepthStencilMatch)( UINT Adapter, D3DDEVTYPE DeviceType, D3DFORMAT AdapterFormat, D3DFORMAT RenderTargetFormat, D3DFORMAT DepthStencilFormat ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, DeviceType, AdapterFormat, RenderTargetFormat, DepthStencilFormat );
		return E_FAIL;
	}
	STDMETHOD(CheckDeviceFormatConversion)( UINT Adapter, D3DDEVTYPE DeviceType, D3DFORMAT SourceFormat, D3DFORMAT TargetFormat ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, DeviceType, SourceFormat, TargetFormat );
		return E_FAIL;
	}
	STDMETHOD(GetDeviceCaps)( UINT Adapter, D3DDEVTYPE DeviceType, D3DCAPS9* pCaps ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, DeviceType, pCaps );
		return E_FAIL;
	}
	STDMETHOD_(HMONITOR, GetAdapterMonitor)( UINT Adapter ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter );
		return {};
	}
	STDMETHOD(CreateDevice)( UINT Adapter, D3DDEVTYPE DeviceType, HWND hFocusWindow, DWORD BehaviorFlags, D3DPRESENT_PARAMETERS* pPresentationParameters, IDirect3DDevice9** ppReturnedDeviceInterface ) override
	{
		D3D9_STUB( "IDirect3D9", Adapter, DeviceType, hFocusWindow, BehaviorFlags, pPresentationParameters, ppReturnedDeviceInterface );
		return E_FAIL;
	}
	STDMETHOD_(UINT, GetAdapterModeCountEx)( UINT Adapter, const D3DDISPLAYMODEFILTER* pFilter ) override
	{
		D3D9_STUB( "IDirect3D9Ex", Adapter, pFilter );
		return {};
	}
	STDMETHOD(EnumAdapterModesEx)( UINT Adapter, const D3DDISPLAYMODEFILTER* pFilter, UINT Mode, D3DDISPLAYMODEEX* pMode ) override
	{
		D3D9_STUB( "IDirect3D9Ex", Adapter, pFilter, Mode, pMode );
		return E_FAIL;
	}
	STDMETHOD(GetAdapterDisplayModeEx)( UINT Adapter, D3DDISPLAYMODEEX* pMode, D3DDISPLAYROTATION* pRotation ) override
	{
		D3D9_STUB( "IDirect3D9Ex", Adapter, pMode, pRotation );
		return E_FAIL;
	}
	STDMETHOD(CreateDeviceEx)( UINT Adapter, D3DDEVTYPE DeviceType, HWND hFocusWindow, DWORD BehaviorFlags, D3DPRESENT_PARAMETERS* pPresentationParameters, D3DDISPLAYMODEEX* pFullscreenDisplayMode, IDirect3DDevice9Ex** ppReturnedDeviceInterface ) override
	{
		D3D9_STUB( "IDirect3D9Ex", Adapter, DeviceType, hFocusWindow, BehaviorFlags, pPresentationParameters, pFullscreenDisplayMode, ppReturnedDeviceInterface );
		return E_FAIL;
	}
	STDMETHOD(GetAdapterLUID)( UINT Adapter, LUID * pLUID ) override
	{
		D3D9_STUB( "IDirect3D9Ex", Adapter, pLUID );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DDevice9Stub : public IDirect3DDevice9
{
public:
	virtual ~CDirect3DDevice9Stub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DDevice9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DDevice9 *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DDevice9", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(TestCooperativeLevel)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return E_FAIL;
	}
	STDMETHOD_(UINT, GetAvailableTextureMem)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return {};
	}
	STDMETHOD(EvictManagedResources)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return E_FAIL;
	}
	STDMETHOD(GetDirect3D)( IDirect3D9** ppD3D9 ) override
	{
		D3D9_STUB( "IDirect3DDevice9", ppD3D9 );
		return E_FAIL;
	}
	STDMETHOD(GetDeviceCaps)( D3DCAPS9* pCaps ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pCaps );
		return E_FAIL;
	}
	STDMETHOD(GetDisplayMode)( UINT iSwapChain, D3DDISPLAYMODE* pMode ) override
	{
		D3D9_STUB( "IDirect3DDevice9", iSwapChain, pMode );
		return E_FAIL;
	}
	STDMETHOD(GetCreationParameters)( D3DDEVICE_CREATION_PARAMETERS * pParameters ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pParameters );
		return E_FAIL;
	}
	STDMETHOD(SetCursorProperties)( UINT XHotSpot, UINT YHotSpot, IDirect3DSurface9* pCursorBitmap ) override
	{
		D3D9_STUB( "IDirect3DDevice9", XHotSpot, YHotSpot, pCursorBitmap );
		return E_FAIL;
	}
	STDMETHOD_(void, SetCursorPosition)( int X, int Y, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DDevice9", X, Y, Flags );
	}
	STDMETHOD_(BOOL, ShowCursor)( BOOL bShow ) override
	{
		D3D9_STUB( "IDirect3DDevice9", bShow );
		return {};
	}
	STDMETHOD(CreateAdditionalSwapChain)( D3DPRESENT_PARAMETERS* pPresentationParameters, IDirect3DSwapChain9** pSwapChain ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pPresentationParameters, pSwapChain );
		return E_FAIL;
	}
	STDMETHOD(GetSwapChain)( UINT iSwapChain, IDirect3DSwapChain9** pSwapChain ) override
	{
		D3D9_STUB( "IDirect3DDevice9", iSwapChain, pSwapChain );
		return E_FAIL;
	}
	STDMETHOD_(UINT, GetNumberOfSwapChains)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return {};
	}
	STDMETHOD(Reset)( D3DPRESENT_PARAMETERS* pPresentationParameters ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pPresentationParameters );
		return E_FAIL;
	}
	STDMETHOD(Present)( const RECT* pSourceRect, const RECT* pDestRect, HWND hDestWindowOverride, const RGNDATA* pDirtyRegion ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pSourceRect, pDestRect, hDestWindowOverride, pDirtyRegion );
		return E_FAIL;
	}
	STDMETHOD(GetBackBuffer)( UINT iSwapChain, UINT iBackBuffer, D3DBACKBUFFER_TYPE Type, IDirect3DSurface9** ppBackBuffer ) override
	{
		D3D9_STUB( "IDirect3DDevice9", iSwapChain, iBackBuffer, Type, ppBackBuffer );
		return E_FAIL;
	}
	STDMETHOD(GetRasterStatus)( UINT iSwapChain, D3DRASTER_STATUS* pRasterStatus ) override
	{
		D3D9_STUB( "IDirect3DDevice9", iSwapChain, pRasterStatus );
		return E_FAIL;
	}
	STDMETHOD(SetDialogBoxMode)( BOOL bEnableDialogs ) override
	{
		D3D9_STUB( "IDirect3DDevice9", bEnableDialogs );
		return E_FAIL;
	}
	STDMETHOD_(void, SetGammaRamp)( UINT iSwapChain, DWORD Flags, const D3DGAMMARAMP* pRamp ) override
	{
		D3D9_STUB( "IDirect3DDevice9", iSwapChain, Flags, pRamp );
	}
	STDMETHOD_(void, GetGammaRamp)( UINT iSwapChain, D3DGAMMARAMP* pRamp ) override
	{
		D3D9_STUB( "IDirect3DDevice9", iSwapChain, pRamp );
	}
	STDMETHOD(CreateTexture)( UINT Width, UINT Height, UINT Levels, DWORD Usage, D3DFORMAT Format, D3DPOOL Pool, IDirect3DTexture9** ppTexture, HANDLE* pSharedHandle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Width, Height, Levels, Usage, Format, Pool, ppTexture, pSharedHandle );
		return E_FAIL;
	}
	STDMETHOD(CreateVolumeTexture)( UINT Width, UINT Height, UINT Depth, UINT Levels, DWORD Usage, D3DFORMAT Format, D3DPOOL Pool, IDirect3DVolumeTexture9** ppVolumeTexture, HANDLE* pSharedHandle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Width, Height, Depth, Levels, Usage, Format, Pool, ppVolumeTexture, pSharedHandle );
		return E_FAIL;
	}
	STDMETHOD(CreateCubeTexture)( UINT EdgeLength, UINT Levels, DWORD Usage, D3DFORMAT Format, D3DPOOL Pool, IDirect3DCubeTexture9** ppCubeTexture, HANDLE* pSharedHandle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", EdgeLength, Levels, Usage, Format, Pool, ppCubeTexture, pSharedHandle );
		return E_FAIL;
	}
	STDMETHOD(CreateVertexBuffer)( UINT Length, DWORD Usage, DWORD FVF, D3DPOOL Pool, IDirect3DVertexBuffer9** ppVertexBuffer, HANDLE* pSharedHandle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Length, Usage, FVF, Pool, ppVertexBuffer, pSharedHandle );
		return E_FAIL;
	}
	STDMETHOD(CreateIndexBuffer)( UINT Length, DWORD Usage, D3DFORMAT Format, D3DPOOL Pool, IDirect3DIndexBuffer9** ppIndexBuffer, HANDLE* pSharedHandle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Length, Usage, Format, Pool, ppIndexBuffer, pSharedHandle );
		return E_FAIL;
	}
	STDMETHOD(CreateRenderTarget)( UINT Width, UINT Height, D3DFORMAT Format, D3DMULTISAMPLE_TYPE MultiSample, DWORD MultisampleQuality, BOOL Lockable, IDirect3DSurface9** ppSurface, HANDLE* pSharedHandle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Width, Height, Format, MultiSample, MultisampleQuality, Lockable, ppSurface, pSharedHandle );
		return E_FAIL;
	}
	STDMETHOD(CreateDepthStencilSurface)( UINT Width, UINT Height, D3DFORMAT Format, D3DMULTISAMPLE_TYPE MultiSample, DWORD MultisampleQuality, BOOL Discard, IDirect3DSurface9** ppSurface, HANDLE* pSharedHandle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Width, Height, Format, MultiSample, MultisampleQuality, Discard, ppSurface, pSharedHandle );
		return E_FAIL;
	}
	STDMETHOD(UpdateSurface)( IDirect3DSurface9* pSourceSurface, const RECT* pSourceRect, IDirect3DSurface9* pDestinationSurface, const POINT* pDestPoint ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pSourceSurface, pSourceRect, pDestinationSurface, pDestPoint );
		return E_FAIL;
	}
	STDMETHOD(UpdateTexture)( IDirect3DBaseTexture9* pSourceTexture, IDirect3DBaseTexture9* pDestinationTexture ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pSourceTexture, pDestinationTexture );
		return E_FAIL;
	}
	STDMETHOD(GetRenderTargetData)( IDirect3DSurface9* pRenderTarget, IDirect3DSurface9* pDestSurface ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pRenderTarget, pDestSurface );
		return E_FAIL;
	}
	STDMETHOD(GetFrontBufferData)( UINT iSwapChain, IDirect3DSurface9* pDestSurface ) override
	{
		D3D9_STUB( "IDirect3DDevice9", iSwapChain, pDestSurface );
		return E_FAIL;
	}
	STDMETHOD(StretchRect)( IDirect3DSurface9* pSourceSurface, const RECT* pSourceRect, IDirect3DSurface9* pDestSurface, const RECT* pDestRect, D3DTEXTUREFILTERTYPE Filter ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pSourceSurface, pSourceRect, pDestSurface, pDestRect, Filter );
		return E_FAIL;
	}
	STDMETHOD(ColorFill)( IDirect3DSurface9* pSurface, const RECT* pRect, D3DCOLOR color ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pSurface, pRect, color );
		return E_FAIL;
	}
	STDMETHOD(CreateOffscreenPlainSurface)( UINT Width, UINT Height, D3DFORMAT Format, D3DPOOL Pool, IDirect3DSurface9** ppSurface, HANDLE* pSharedHandle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Width, Height, Format, Pool, ppSurface, pSharedHandle );
		return E_FAIL;
	}
	STDMETHOD(SetRenderTarget)( DWORD RenderTargetIndex, IDirect3DSurface9* pRenderTarget ) override
	{
		D3D9_STUB( "IDirect3DDevice9", RenderTargetIndex, pRenderTarget );
		return E_FAIL;
	}
	STDMETHOD(GetRenderTarget)( DWORD RenderTargetIndex, IDirect3DSurface9** ppRenderTarget ) override
	{
		D3D9_STUB( "IDirect3DDevice9", RenderTargetIndex, ppRenderTarget );
		return E_FAIL;
	}
	STDMETHOD(SetDepthStencilSurface)( IDirect3DSurface9* pNewZStencil ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pNewZStencil );
		return E_FAIL;
	}
	STDMETHOD(GetDepthStencilSurface)( IDirect3DSurface9** ppZStencilSurface ) override
	{
		D3D9_STUB( "IDirect3DDevice9", ppZStencilSurface );
		return E_FAIL;
	}
	STDMETHOD(BeginScene)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return E_FAIL;
	}
	STDMETHOD(EndScene)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return E_FAIL;
	}
	STDMETHOD(Clear)( DWORD Count, const D3DRECT* pRects, DWORD Flags, D3DCOLOR Color, float Z, DWORD Stencil ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Count, pRects, Flags, Color, Z, Stencil );
		return E_FAIL;
	}
	STDMETHOD(SetTransform)( D3DTRANSFORMSTATETYPE State, const D3DMATRIX* pMatrix ) override
	{
		D3D9_STUB( "IDirect3DDevice9", State, pMatrix );
		return E_FAIL;
	}
	STDMETHOD(GetTransform)( D3DTRANSFORMSTATETYPE State, D3DMATRIX* pMatrix ) override
	{
		D3D9_STUB( "IDirect3DDevice9", State, pMatrix );
		return E_FAIL;
	}
	STDMETHOD(MultiplyTransform)( D3DTRANSFORMSTATETYPE arg0, const D3DMATRIX* arg1 ) override
	{
		D3D9_STUB( "IDirect3DDevice9", arg0, arg1 );
		return E_FAIL;
	}
	STDMETHOD(SetViewport)( const D3DVIEWPORT9* pViewport ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pViewport );
		return E_FAIL;
	}
	STDMETHOD(GetViewport)( D3DVIEWPORT9* pViewport ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pViewport );
		return E_FAIL;
	}
	STDMETHOD(SetMaterial)( const D3DMATERIAL9* pMaterial ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pMaterial );
		return E_FAIL;
	}
	STDMETHOD(GetMaterial)( D3DMATERIAL9* pMaterial ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pMaterial );
		return E_FAIL;
	}
	STDMETHOD(SetLight)( DWORD Index, const D3DLIGHT9* arg1 ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Index, arg1 );
		return E_FAIL;
	}
	STDMETHOD(GetLight)( DWORD Index, D3DLIGHT9* arg1 ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Index, arg1 );
		return E_FAIL;
	}
	STDMETHOD(LightEnable)( DWORD Index, BOOL Enable ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Index, Enable );
		return E_FAIL;
	}
	STDMETHOD(GetLightEnable)( DWORD Index, BOOL* pEnable ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Index, pEnable );
		return E_FAIL;
	}
	STDMETHOD(SetClipPlane)( DWORD Index, const float* pPlane ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Index, pPlane );
		return E_FAIL;
	}
	STDMETHOD(GetClipPlane)( DWORD Index, float* pPlane ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Index, pPlane );
		return E_FAIL;
	}
	STDMETHOD(SetRenderState)( D3DRENDERSTATETYPE State, DWORD Value ) override
	{
		D3D9_STUB( "IDirect3DDevice9", State, Value );
		return E_FAIL;
	}
	STDMETHOD(GetRenderState)( D3DRENDERSTATETYPE State, DWORD* pValue ) override
	{
		D3D9_STUB( "IDirect3DDevice9", State, pValue );
		return E_FAIL;
	}
	STDMETHOD(CreateStateBlock)( D3DSTATEBLOCKTYPE Type, IDirect3DStateBlock9** ppSB ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Type, ppSB );
		return E_FAIL;
	}
	STDMETHOD(BeginStateBlock)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return E_FAIL;
	}
	STDMETHOD(EndStateBlock)( IDirect3DStateBlock9** ppSB ) override
	{
		D3D9_STUB( "IDirect3DDevice9", ppSB );
		return E_FAIL;
	}
	STDMETHOD(SetClipStatus)( const D3DCLIPSTATUS9* pClipStatus ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pClipStatus );
		return E_FAIL;
	}
	STDMETHOD(GetClipStatus)( D3DCLIPSTATUS9* pClipStatus ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pClipStatus );
		return E_FAIL;
	}
	STDMETHOD(GetTexture)( DWORD Stage, IDirect3DBaseTexture9** ppTexture ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Stage, ppTexture );
		return E_FAIL;
	}
	STDMETHOD(SetTexture)( DWORD Stage, IDirect3DBaseTexture9* pTexture ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Stage, pTexture );
		return E_FAIL;
	}
	STDMETHOD(GetTextureStageState)( DWORD Stage, D3DTEXTURESTAGESTATETYPE Type, DWORD* pValue ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Stage, Type, pValue );
		return E_FAIL;
	}
	STDMETHOD(SetTextureStageState)( DWORD Stage, D3DTEXTURESTAGESTATETYPE Type, DWORD Value ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Stage, Type, Value );
		return E_FAIL;
	}
	STDMETHOD(GetSamplerState)( DWORD Sampler, D3DSAMPLERSTATETYPE Type, DWORD* pValue ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Sampler, Type, pValue );
		return E_FAIL;
	}
	STDMETHOD(SetSamplerState)( DWORD Sampler, D3DSAMPLERSTATETYPE Type, DWORD Value ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Sampler, Type, Value );
		return E_FAIL;
	}
	STDMETHOD(ValidateDevice)( DWORD* pNumPasses ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pNumPasses );
		return E_FAIL;
	}
	STDMETHOD(SetPaletteEntries)( UINT PaletteNumber, const PALETTEENTRY* pEntries ) override
	{
		D3D9_STUB( "IDirect3DDevice9", PaletteNumber, pEntries );
		return E_FAIL;
	}
	STDMETHOD(GetPaletteEntries)( UINT PaletteNumber, PALETTEENTRY* pEntries ) override
	{
		D3D9_STUB( "IDirect3DDevice9", PaletteNumber, pEntries );
		return E_FAIL;
	}
	STDMETHOD(SetCurrentTexturePalette)( UINT PaletteNumber ) override
	{
		D3D9_STUB( "IDirect3DDevice9", PaletteNumber );
		return E_FAIL;
	}
	STDMETHOD(GetCurrentTexturePalette)( UINT * PaletteNumber ) override
	{
		D3D9_STUB( "IDirect3DDevice9", PaletteNumber );
		return E_FAIL;
	}
	STDMETHOD(SetScissorRect)( const RECT* pRect ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pRect );
		return E_FAIL;
	}
	STDMETHOD(GetScissorRect)( RECT* pRect ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pRect );
		return E_FAIL;
	}
	STDMETHOD(SetSoftwareVertexProcessing)( BOOL bSoftware ) override
	{
		D3D9_STUB( "IDirect3DDevice9", bSoftware );
		return E_FAIL;
	}
	STDMETHOD_(BOOL, GetSoftwareVertexProcessing)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return {};
	}
	STDMETHOD(SetNPatchMode)( float nSegments ) override
	{
		D3D9_STUB( "IDirect3DDevice9", nSegments );
		return E_FAIL;
	}
	STDMETHOD_(float, GetNPatchMode)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return {};
	}
	STDMETHOD(DrawPrimitive)( D3DPRIMITIVETYPE PrimitiveType, UINT StartVertex, UINT PrimitiveCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", PrimitiveType, StartVertex, PrimitiveCount );
		return E_FAIL;
	}
	STDMETHOD(DrawIndexedPrimitive)( D3DPRIMITIVETYPE arg0, INT BaseVertexIndex, UINT MinVertexIndex, UINT NumVertices, UINT startIndex, UINT primCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", arg0, BaseVertexIndex, MinVertexIndex, NumVertices, startIndex, primCount );
		return E_FAIL;
	}
	STDMETHOD(DrawPrimitiveUP)( D3DPRIMITIVETYPE PrimitiveType, UINT PrimitiveCount, const void* pVertexStreamZeroData, UINT VertexStreamZeroStride ) override
	{
		D3D9_STUB( "IDirect3DDevice9", PrimitiveType, PrimitiveCount, pVertexStreamZeroData, VertexStreamZeroStride );
		return E_FAIL;
	}
	STDMETHOD(DrawIndexedPrimitiveUP)( D3DPRIMITIVETYPE PrimitiveType, UINT MinVertexIndex, UINT NumVertices, UINT PrimitiveCount, const void* pIndexData, D3DFORMAT IndexDataFormat, const void* pVertexStreamZeroData, UINT VertexStreamZeroStride ) override
	{
		D3D9_STUB( "IDirect3DDevice9", PrimitiveType, MinVertexIndex, NumVertices, PrimitiveCount, pIndexData, IndexDataFormat, pVertexStreamZeroData, VertexStreamZeroStride );
		return E_FAIL;
	}
	STDMETHOD(ProcessVertices)( UINT SrcStartIndex, UINT DestIndex, UINT VertexCount, IDirect3DVertexBuffer9* pDestBuffer, IDirect3DVertexDeclaration9* pVertexDecl, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DDevice9", SrcStartIndex, DestIndex, VertexCount, pDestBuffer, pVertexDecl, Flags );
		return E_FAIL;
	}
	STDMETHOD(CreateVertexDeclaration)( const D3DVERTEXELEMENT9* pVertexElements, IDirect3DVertexDeclaration9** ppDecl ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pVertexElements, ppDecl );
		return E_FAIL;
	}
	STDMETHOD(SetVertexDeclaration)( IDirect3DVertexDeclaration9* pDecl ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pDecl );
		return E_FAIL;
	}
	STDMETHOD(GetVertexDeclaration)( IDirect3DVertexDeclaration9** ppDecl ) override
	{
		D3D9_STUB( "IDirect3DDevice9", ppDecl );
		return E_FAIL;
	}
	STDMETHOD(SetFVF)( DWORD FVF ) override
	{
		D3D9_STUB( "IDirect3DDevice9", FVF );
		return E_FAIL;
	}
	STDMETHOD(GetFVF)( DWORD* pFVF ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pFVF );
		return E_FAIL;
	}
	STDMETHOD(CreateVertexShader)( const DWORD* pFunction, IDirect3DVertexShader9** ppShader ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pFunction, ppShader );
		return E_FAIL;
	}
	STDMETHOD(SetVertexShader)( IDirect3DVertexShader9* pShader ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pShader );
		return E_FAIL;
	}
	STDMETHOD(GetVertexShader)( IDirect3DVertexShader9** ppShader ) override
	{
		D3D9_STUB( "IDirect3DDevice9", ppShader );
		return E_FAIL;
	}
	STDMETHOD(SetVertexShaderConstantF)( UINT StartRegister, const float* pConstantData, UINT Vector4fCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, Vector4fCount );
		return E_FAIL;
	}
	STDMETHOD(GetVertexShaderConstantF)( UINT StartRegister, float* pConstantData, UINT Vector4fCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, Vector4fCount );
		return E_FAIL;
	}
	STDMETHOD(SetVertexShaderConstantI)( UINT StartRegister, const int* pConstantData, UINT Vector4iCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, Vector4iCount );
		return E_FAIL;
	}
	STDMETHOD(GetVertexShaderConstantI)( UINT StartRegister, int* pConstantData, UINT Vector4iCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, Vector4iCount );
		return E_FAIL;
	}
	STDMETHOD(SetVertexShaderConstantB)( UINT StartRegister, const BOOL* pConstantData, UINT BoolCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, BoolCount );
		return E_FAIL;
	}
	STDMETHOD(GetVertexShaderConstantB)( UINT StartRegister, BOOL* pConstantData, UINT BoolCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, BoolCount );
		return E_FAIL;
	}
	STDMETHOD(SetStreamSource)( UINT StreamNumber, IDirect3DVertexBuffer9* pStreamData, UINT OffsetInBytes, UINT Stride ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StreamNumber, pStreamData, OffsetInBytes, Stride );
		return E_FAIL;
	}
	STDMETHOD(GetStreamSource)( UINT StreamNumber, IDirect3DVertexBuffer9** ppStreamData, UINT* pOffsetInBytes, UINT* pStride ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StreamNumber, ppStreamData, pOffsetInBytes, pStride );
		return E_FAIL;
	}
	STDMETHOD(SetStreamSourceFreq)( UINT StreamNumber, UINT Setting ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StreamNumber, Setting );
		return E_FAIL;
	}
	STDMETHOD(GetStreamSourceFreq)( UINT StreamNumber, UINT* pSetting ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StreamNumber, pSetting );
		return E_FAIL;
	}
	STDMETHOD(SetIndices)( IDirect3DIndexBuffer9* pIndexData ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pIndexData );
		return E_FAIL;
	}
	STDMETHOD(GetIndices)( IDirect3DIndexBuffer9** ppIndexData ) override
	{
		D3D9_STUB( "IDirect3DDevice9", ppIndexData );
		return E_FAIL;
	}
	STDMETHOD(CreatePixelShader)( const DWORD* pFunction, IDirect3DPixelShader9** ppShader ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pFunction, ppShader );
		return E_FAIL;
	}
	STDMETHOD(SetPixelShader)( IDirect3DPixelShader9* pShader ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pShader );
		return E_FAIL;
	}
	STDMETHOD(GetPixelShader)( IDirect3DPixelShader9** ppShader ) override
	{
		D3D9_STUB( "IDirect3DDevice9", ppShader );
		return E_FAIL;
	}
	STDMETHOD(SetPixelShaderConstantF)( UINT StartRegister, const float* pConstantData, UINT Vector4fCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, Vector4fCount );
		return E_FAIL;
	}
	STDMETHOD(GetPixelShaderConstantF)( UINT StartRegister, float* pConstantData, UINT Vector4fCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, Vector4fCount );
		return E_FAIL;
	}
	STDMETHOD(SetPixelShaderConstantI)( UINT StartRegister, const int* pConstantData, UINT Vector4iCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, Vector4iCount );
		return E_FAIL;
	}
	STDMETHOD(GetPixelShaderConstantI)( UINT StartRegister, int* pConstantData, UINT Vector4iCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, Vector4iCount );
		return E_FAIL;
	}
	STDMETHOD(SetPixelShaderConstantB)( UINT StartRegister, const BOOL* pConstantData, UINT BoolCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, BoolCount );
		return E_FAIL;
	}
	STDMETHOD(GetPixelShaderConstantB)( UINT StartRegister, BOOL* pConstantData, UINT BoolCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, BoolCount );
		return E_FAIL;
	}
	STDMETHOD(DrawRectPatch)( UINT Handle, const float* pNumSegs, const D3DRECTPATCH_INFO* pRectPatchInfo ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Handle, pNumSegs, pRectPatchInfo );
		return E_FAIL;
	}
	STDMETHOD(DrawTriPatch)( UINT Handle, const float* pNumSegs, const D3DTRIPATCH_INFO* pTriPatchInfo ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Handle, pNumSegs, pTriPatchInfo );
		return E_FAIL;
	}
	STDMETHOD(DeletePatch)( UINT Handle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Handle );
		return E_FAIL;
	}
	STDMETHOD(CreateQuery)( D3DQUERYTYPE Type, IDirect3DQuery9** ppQuery ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Type, ppQuery );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DDevice9ExStub : public IDirect3DDevice9Ex
{
public:
	virtual ~CDirect3DDevice9ExStub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DDevice9Ex || riid == IID_IDirect3DDevice9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DDevice9Ex *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DDevice9Ex", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(TestCooperativeLevel)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return E_FAIL;
	}
	STDMETHOD_(UINT, GetAvailableTextureMem)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return {};
	}
	STDMETHOD(EvictManagedResources)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return E_FAIL;
	}
	STDMETHOD(GetDirect3D)( IDirect3D9** ppD3D9 ) override
	{
		D3D9_STUB( "IDirect3DDevice9", ppD3D9 );
		return E_FAIL;
	}
	STDMETHOD(GetDeviceCaps)( D3DCAPS9* pCaps ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pCaps );
		return E_FAIL;
	}
	STDMETHOD(GetDisplayMode)( UINT iSwapChain, D3DDISPLAYMODE* pMode ) override
	{
		D3D9_STUB( "IDirect3DDevice9", iSwapChain, pMode );
		return E_FAIL;
	}
	STDMETHOD(GetCreationParameters)( D3DDEVICE_CREATION_PARAMETERS * pParameters ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pParameters );
		return E_FAIL;
	}
	STDMETHOD(SetCursorProperties)( UINT XHotSpot, UINT YHotSpot, IDirect3DSurface9* pCursorBitmap ) override
	{
		D3D9_STUB( "IDirect3DDevice9", XHotSpot, YHotSpot, pCursorBitmap );
		return E_FAIL;
	}
	STDMETHOD_(void, SetCursorPosition)( int X, int Y, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DDevice9", X, Y, Flags );
	}
	STDMETHOD_(BOOL, ShowCursor)( BOOL bShow ) override
	{
		D3D9_STUB( "IDirect3DDevice9", bShow );
		return {};
	}
	STDMETHOD(CreateAdditionalSwapChain)( D3DPRESENT_PARAMETERS* pPresentationParameters, IDirect3DSwapChain9** pSwapChain ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pPresentationParameters, pSwapChain );
		return E_FAIL;
	}
	STDMETHOD(GetSwapChain)( UINT iSwapChain, IDirect3DSwapChain9** pSwapChain ) override
	{
		D3D9_STUB( "IDirect3DDevice9", iSwapChain, pSwapChain );
		return E_FAIL;
	}
	STDMETHOD_(UINT, GetNumberOfSwapChains)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return {};
	}
	STDMETHOD(Reset)( D3DPRESENT_PARAMETERS* pPresentationParameters ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pPresentationParameters );
		return E_FAIL;
	}
	STDMETHOD(Present)( const RECT* pSourceRect, const RECT* pDestRect, HWND hDestWindowOverride, const RGNDATA* pDirtyRegion ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pSourceRect, pDestRect, hDestWindowOverride, pDirtyRegion );
		return E_FAIL;
	}
	STDMETHOD(GetBackBuffer)( UINT iSwapChain, UINT iBackBuffer, D3DBACKBUFFER_TYPE Type, IDirect3DSurface9** ppBackBuffer ) override
	{
		D3D9_STUB( "IDirect3DDevice9", iSwapChain, iBackBuffer, Type, ppBackBuffer );
		return E_FAIL;
	}
	STDMETHOD(GetRasterStatus)( UINT iSwapChain, D3DRASTER_STATUS* pRasterStatus ) override
	{
		D3D9_STUB( "IDirect3DDevice9", iSwapChain, pRasterStatus );
		return E_FAIL;
	}
	STDMETHOD(SetDialogBoxMode)( BOOL bEnableDialogs ) override
	{
		D3D9_STUB( "IDirect3DDevice9", bEnableDialogs );
		return E_FAIL;
	}
	STDMETHOD_(void, SetGammaRamp)( UINT iSwapChain, DWORD Flags, const D3DGAMMARAMP* pRamp ) override
	{
		D3D9_STUB( "IDirect3DDevice9", iSwapChain, Flags, pRamp );
	}
	STDMETHOD_(void, GetGammaRamp)( UINT iSwapChain, D3DGAMMARAMP* pRamp ) override
	{
		D3D9_STUB( "IDirect3DDevice9", iSwapChain, pRamp );
	}
	STDMETHOD(CreateTexture)( UINT Width, UINT Height, UINT Levels, DWORD Usage, D3DFORMAT Format, D3DPOOL Pool, IDirect3DTexture9** ppTexture, HANDLE* pSharedHandle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Width, Height, Levels, Usage, Format, Pool, ppTexture, pSharedHandle );
		return E_FAIL;
	}
	STDMETHOD(CreateVolumeTexture)( UINT Width, UINT Height, UINT Depth, UINT Levels, DWORD Usage, D3DFORMAT Format, D3DPOOL Pool, IDirect3DVolumeTexture9** ppVolumeTexture, HANDLE* pSharedHandle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Width, Height, Depth, Levels, Usage, Format, Pool, ppVolumeTexture, pSharedHandle );
		return E_FAIL;
	}
	STDMETHOD(CreateCubeTexture)( UINT EdgeLength, UINT Levels, DWORD Usage, D3DFORMAT Format, D3DPOOL Pool, IDirect3DCubeTexture9** ppCubeTexture, HANDLE* pSharedHandle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", EdgeLength, Levels, Usage, Format, Pool, ppCubeTexture, pSharedHandle );
		return E_FAIL;
	}
	STDMETHOD(CreateVertexBuffer)( UINT Length, DWORD Usage, DWORD FVF, D3DPOOL Pool, IDirect3DVertexBuffer9** ppVertexBuffer, HANDLE* pSharedHandle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Length, Usage, FVF, Pool, ppVertexBuffer, pSharedHandle );
		return E_FAIL;
	}
	STDMETHOD(CreateIndexBuffer)( UINT Length, DWORD Usage, D3DFORMAT Format, D3DPOOL Pool, IDirect3DIndexBuffer9** ppIndexBuffer, HANDLE* pSharedHandle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Length, Usage, Format, Pool, ppIndexBuffer, pSharedHandle );
		return E_FAIL;
	}
	STDMETHOD(CreateRenderTarget)( UINT Width, UINT Height, D3DFORMAT Format, D3DMULTISAMPLE_TYPE MultiSample, DWORD MultisampleQuality, BOOL Lockable, IDirect3DSurface9** ppSurface, HANDLE* pSharedHandle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Width, Height, Format, MultiSample, MultisampleQuality, Lockable, ppSurface, pSharedHandle );
		return E_FAIL;
	}
	STDMETHOD(CreateDepthStencilSurface)( UINT Width, UINT Height, D3DFORMAT Format, D3DMULTISAMPLE_TYPE MultiSample, DWORD MultisampleQuality, BOOL Discard, IDirect3DSurface9** ppSurface, HANDLE* pSharedHandle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Width, Height, Format, MultiSample, MultisampleQuality, Discard, ppSurface, pSharedHandle );
		return E_FAIL;
	}
	STDMETHOD(UpdateSurface)( IDirect3DSurface9* pSourceSurface, const RECT* pSourceRect, IDirect3DSurface9* pDestinationSurface, const POINT* pDestPoint ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pSourceSurface, pSourceRect, pDestinationSurface, pDestPoint );
		return E_FAIL;
	}
	STDMETHOD(UpdateTexture)( IDirect3DBaseTexture9* pSourceTexture, IDirect3DBaseTexture9* pDestinationTexture ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pSourceTexture, pDestinationTexture );
		return E_FAIL;
	}
	STDMETHOD(GetRenderTargetData)( IDirect3DSurface9* pRenderTarget, IDirect3DSurface9* pDestSurface ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pRenderTarget, pDestSurface );
		return E_FAIL;
	}
	STDMETHOD(GetFrontBufferData)( UINT iSwapChain, IDirect3DSurface9* pDestSurface ) override
	{
		D3D9_STUB( "IDirect3DDevice9", iSwapChain, pDestSurface );
		return E_FAIL;
	}
	STDMETHOD(StretchRect)( IDirect3DSurface9* pSourceSurface, const RECT* pSourceRect, IDirect3DSurface9* pDestSurface, const RECT* pDestRect, D3DTEXTUREFILTERTYPE Filter ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pSourceSurface, pSourceRect, pDestSurface, pDestRect, Filter );
		return E_FAIL;
	}
	STDMETHOD(ColorFill)( IDirect3DSurface9* pSurface, const RECT* pRect, D3DCOLOR color ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pSurface, pRect, color );
		return E_FAIL;
	}
	STDMETHOD(CreateOffscreenPlainSurface)( UINT Width, UINT Height, D3DFORMAT Format, D3DPOOL Pool, IDirect3DSurface9** ppSurface, HANDLE* pSharedHandle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Width, Height, Format, Pool, ppSurface, pSharedHandle );
		return E_FAIL;
	}
	STDMETHOD(SetRenderTarget)( DWORD RenderTargetIndex, IDirect3DSurface9* pRenderTarget ) override
	{
		D3D9_STUB( "IDirect3DDevice9", RenderTargetIndex, pRenderTarget );
		return E_FAIL;
	}
	STDMETHOD(GetRenderTarget)( DWORD RenderTargetIndex, IDirect3DSurface9** ppRenderTarget ) override
	{
		D3D9_STUB( "IDirect3DDevice9", RenderTargetIndex, ppRenderTarget );
		return E_FAIL;
	}
	STDMETHOD(SetDepthStencilSurface)( IDirect3DSurface9* pNewZStencil ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pNewZStencil );
		return E_FAIL;
	}
	STDMETHOD(GetDepthStencilSurface)( IDirect3DSurface9** ppZStencilSurface ) override
	{
		D3D9_STUB( "IDirect3DDevice9", ppZStencilSurface );
		return E_FAIL;
	}
	STDMETHOD(BeginScene)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return E_FAIL;
	}
	STDMETHOD(EndScene)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return E_FAIL;
	}
	STDMETHOD(Clear)( DWORD Count, const D3DRECT* pRects, DWORD Flags, D3DCOLOR Color, float Z, DWORD Stencil ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Count, pRects, Flags, Color, Z, Stencil );
		return E_FAIL;
	}
	STDMETHOD(SetTransform)( D3DTRANSFORMSTATETYPE State, const D3DMATRIX* pMatrix ) override
	{
		D3D9_STUB( "IDirect3DDevice9", State, pMatrix );
		return E_FAIL;
	}
	STDMETHOD(GetTransform)( D3DTRANSFORMSTATETYPE State, D3DMATRIX* pMatrix ) override
	{
		D3D9_STUB( "IDirect3DDevice9", State, pMatrix );
		return E_FAIL;
	}
	STDMETHOD(MultiplyTransform)( D3DTRANSFORMSTATETYPE arg0, const D3DMATRIX* arg1 ) override
	{
		D3D9_STUB( "IDirect3DDevice9", arg0, arg1 );
		return E_FAIL;
	}
	STDMETHOD(SetViewport)( const D3DVIEWPORT9* pViewport ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pViewport );
		return E_FAIL;
	}
	STDMETHOD(GetViewport)( D3DVIEWPORT9* pViewport ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pViewport );
		return E_FAIL;
	}
	STDMETHOD(SetMaterial)( const D3DMATERIAL9* pMaterial ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pMaterial );
		return E_FAIL;
	}
	STDMETHOD(GetMaterial)( D3DMATERIAL9* pMaterial ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pMaterial );
		return E_FAIL;
	}
	STDMETHOD(SetLight)( DWORD Index, const D3DLIGHT9* arg1 ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Index, arg1 );
		return E_FAIL;
	}
	STDMETHOD(GetLight)( DWORD Index, D3DLIGHT9* arg1 ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Index, arg1 );
		return E_FAIL;
	}
	STDMETHOD(LightEnable)( DWORD Index, BOOL Enable ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Index, Enable );
		return E_FAIL;
	}
	STDMETHOD(GetLightEnable)( DWORD Index, BOOL* pEnable ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Index, pEnable );
		return E_FAIL;
	}
	STDMETHOD(SetClipPlane)( DWORD Index, const float* pPlane ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Index, pPlane );
		return E_FAIL;
	}
	STDMETHOD(GetClipPlane)( DWORD Index, float* pPlane ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Index, pPlane );
		return E_FAIL;
	}
	STDMETHOD(SetRenderState)( D3DRENDERSTATETYPE State, DWORD Value ) override
	{
		D3D9_STUB( "IDirect3DDevice9", State, Value );
		return E_FAIL;
	}
	STDMETHOD(GetRenderState)( D3DRENDERSTATETYPE State, DWORD* pValue ) override
	{
		D3D9_STUB( "IDirect3DDevice9", State, pValue );
		return E_FAIL;
	}
	STDMETHOD(CreateStateBlock)( D3DSTATEBLOCKTYPE Type, IDirect3DStateBlock9** ppSB ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Type, ppSB );
		return E_FAIL;
	}
	STDMETHOD(BeginStateBlock)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return E_FAIL;
	}
	STDMETHOD(EndStateBlock)( IDirect3DStateBlock9** ppSB ) override
	{
		D3D9_STUB( "IDirect3DDevice9", ppSB );
		return E_FAIL;
	}
	STDMETHOD(SetClipStatus)( const D3DCLIPSTATUS9* pClipStatus ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pClipStatus );
		return E_FAIL;
	}
	STDMETHOD(GetClipStatus)( D3DCLIPSTATUS9* pClipStatus ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pClipStatus );
		return E_FAIL;
	}
	STDMETHOD(GetTexture)( DWORD Stage, IDirect3DBaseTexture9** ppTexture ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Stage, ppTexture );
		return E_FAIL;
	}
	STDMETHOD(SetTexture)( DWORD Stage, IDirect3DBaseTexture9* pTexture ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Stage, pTexture );
		return E_FAIL;
	}
	STDMETHOD(GetTextureStageState)( DWORD Stage, D3DTEXTURESTAGESTATETYPE Type, DWORD* pValue ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Stage, Type, pValue );
		return E_FAIL;
	}
	STDMETHOD(SetTextureStageState)( DWORD Stage, D3DTEXTURESTAGESTATETYPE Type, DWORD Value ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Stage, Type, Value );
		return E_FAIL;
	}
	STDMETHOD(GetSamplerState)( DWORD Sampler, D3DSAMPLERSTATETYPE Type, DWORD* pValue ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Sampler, Type, pValue );
		return E_FAIL;
	}
	STDMETHOD(SetSamplerState)( DWORD Sampler, D3DSAMPLERSTATETYPE Type, DWORD Value ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Sampler, Type, Value );
		return E_FAIL;
	}
	STDMETHOD(ValidateDevice)( DWORD* pNumPasses ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pNumPasses );
		return E_FAIL;
	}
	STDMETHOD(SetPaletteEntries)( UINT PaletteNumber, const PALETTEENTRY* pEntries ) override
	{
		D3D9_STUB( "IDirect3DDevice9", PaletteNumber, pEntries );
		return E_FAIL;
	}
	STDMETHOD(GetPaletteEntries)( UINT PaletteNumber, PALETTEENTRY* pEntries ) override
	{
		D3D9_STUB( "IDirect3DDevice9", PaletteNumber, pEntries );
		return E_FAIL;
	}
	STDMETHOD(SetCurrentTexturePalette)( UINT PaletteNumber ) override
	{
		D3D9_STUB( "IDirect3DDevice9", PaletteNumber );
		return E_FAIL;
	}
	STDMETHOD(GetCurrentTexturePalette)( UINT * PaletteNumber ) override
	{
		D3D9_STUB( "IDirect3DDevice9", PaletteNumber );
		return E_FAIL;
	}
	STDMETHOD(SetScissorRect)( const RECT* pRect ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pRect );
		return E_FAIL;
	}
	STDMETHOD(GetScissorRect)( RECT* pRect ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pRect );
		return E_FAIL;
	}
	STDMETHOD(SetSoftwareVertexProcessing)( BOOL bSoftware ) override
	{
		D3D9_STUB( "IDirect3DDevice9", bSoftware );
		return E_FAIL;
	}
	STDMETHOD_(BOOL, GetSoftwareVertexProcessing)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return {};
	}
	STDMETHOD(SetNPatchMode)( float nSegments ) override
	{
		D3D9_STUB( "IDirect3DDevice9", nSegments );
		return E_FAIL;
	}
	STDMETHOD_(float, GetNPatchMode)() override
	{
		D3D9_STUB0( "IDirect3DDevice9" );
		return {};
	}
	STDMETHOD(DrawPrimitive)( D3DPRIMITIVETYPE PrimitiveType, UINT StartVertex, UINT PrimitiveCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", PrimitiveType, StartVertex, PrimitiveCount );
		return E_FAIL;
	}
	STDMETHOD(DrawIndexedPrimitive)( D3DPRIMITIVETYPE arg0, INT BaseVertexIndex, UINT MinVertexIndex, UINT NumVertices, UINT startIndex, UINT primCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", arg0, BaseVertexIndex, MinVertexIndex, NumVertices, startIndex, primCount );
		return E_FAIL;
	}
	STDMETHOD(DrawPrimitiveUP)( D3DPRIMITIVETYPE PrimitiveType, UINT PrimitiveCount, const void* pVertexStreamZeroData, UINT VertexStreamZeroStride ) override
	{
		D3D9_STUB( "IDirect3DDevice9", PrimitiveType, PrimitiveCount, pVertexStreamZeroData, VertexStreamZeroStride );
		return E_FAIL;
	}
	STDMETHOD(DrawIndexedPrimitiveUP)( D3DPRIMITIVETYPE PrimitiveType, UINT MinVertexIndex, UINT NumVertices, UINT PrimitiveCount, const void* pIndexData, D3DFORMAT IndexDataFormat, const void* pVertexStreamZeroData, UINT VertexStreamZeroStride ) override
	{
		D3D9_STUB( "IDirect3DDevice9", PrimitiveType, MinVertexIndex, NumVertices, PrimitiveCount, pIndexData, IndexDataFormat, pVertexStreamZeroData, VertexStreamZeroStride );
		return E_FAIL;
	}
	STDMETHOD(ProcessVertices)( UINT SrcStartIndex, UINT DestIndex, UINT VertexCount, IDirect3DVertexBuffer9* pDestBuffer, IDirect3DVertexDeclaration9* pVertexDecl, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DDevice9", SrcStartIndex, DestIndex, VertexCount, pDestBuffer, pVertexDecl, Flags );
		return E_FAIL;
	}
	STDMETHOD(CreateVertexDeclaration)( const D3DVERTEXELEMENT9* pVertexElements, IDirect3DVertexDeclaration9** ppDecl ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pVertexElements, ppDecl );
		return E_FAIL;
	}
	STDMETHOD(SetVertexDeclaration)( IDirect3DVertexDeclaration9* pDecl ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pDecl );
		return E_FAIL;
	}
	STDMETHOD(GetVertexDeclaration)( IDirect3DVertexDeclaration9** ppDecl ) override
	{
		D3D9_STUB( "IDirect3DDevice9", ppDecl );
		return E_FAIL;
	}
	STDMETHOD(SetFVF)( DWORD FVF ) override
	{
		D3D9_STUB( "IDirect3DDevice9", FVF );
		return E_FAIL;
	}
	STDMETHOD(GetFVF)( DWORD* pFVF ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pFVF );
		return E_FAIL;
	}
	STDMETHOD(CreateVertexShader)( const DWORD* pFunction, IDirect3DVertexShader9** ppShader ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pFunction, ppShader );
		return E_FAIL;
	}
	STDMETHOD(SetVertexShader)( IDirect3DVertexShader9* pShader ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pShader );
		return E_FAIL;
	}
	STDMETHOD(GetVertexShader)( IDirect3DVertexShader9** ppShader ) override
	{
		D3D9_STUB( "IDirect3DDevice9", ppShader );
		return E_FAIL;
	}
	STDMETHOD(SetVertexShaderConstantF)( UINT StartRegister, const float* pConstantData, UINT Vector4fCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, Vector4fCount );
		return E_FAIL;
	}
	STDMETHOD(GetVertexShaderConstantF)( UINT StartRegister, float* pConstantData, UINT Vector4fCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, Vector4fCount );
		return E_FAIL;
	}
	STDMETHOD(SetVertexShaderConstantI)( UINT StartRegister, const int* pConstantData, UINT Vector4iCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, Vector4iCount );
		return E_FAIL;
	}
	STDMETHOD(GetVertexShaderConstantI)( UINT StartRegister, int* pConstantData, UINT Vector4iCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, Vector4iCount );
		return E_FAIL;
	}
	STDMETHOD(SetVertexShaderConstantB)( UINT StartRegister, const BOOL* pConstantData, UINT BoolCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, BoolCount );
		return E_FAIL;
	}
	STDMETHOD(GetVertexShaderConstantB)( UINT StartRegister, BOOL* pConstantData, UINT BoolCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, BoolCount );
		return E_FAIL;
	}
	STDMETHOD(SetStreamSource)( UINT StreamNumber, IDirect3DVertexBuffer9* pStreamData, UINT OffsetInBytes, UINT Stride ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StreamNumber, pStreamData, OffsetInBytes, Stride );
		return E_FAIL;
	}
	STDMETHOD(GetStreamSource)( UINT StreamNumber, IDirect3DVertexBuffer9** ppStreamData, UINT* pOffsetInBytes, UINT* pStride ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StreamNumber, ppStreamData, pOffsetInBytes, pStride );
		return E_FAIL;
	}
	STDMETHOD(SetStreamSourceFreq)( UINT StreamNumber, UINT Setting ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StreamNumber, Setting );
		return E_FAIL;
	}
	STDMETHOD(GetStreamSourceFreq)( UINT StreamNumber, UINT* pSetting ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StreamNumber, pSetting );
		return E_FAIL;
	}
	STDMETHOD(SetIndices)( IDirect3DIndexBuffer9* pIndexData ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pIndexData );
		return E_FAIL;
	}
	STDMETHOD(GetIndices)( IDirect3DIndexBuffer9** ppIndexData ) override
	{
		D3D9_STUB( "IDirect3DDevice9", ppIndexData );
		return E_FAIL;
	}
	STDMETHOD(CreatePixelShader)( const DWORD* pFunction, IDirect3DPixelShader9** ppShader ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pFunction, ppShader );
		return E_FAIL;
	}
	STDMETHOD(SetPixelShader)( IDirect3DPixelShader9* pShader ) override
	{
		D3D9_STUB( "IDirect3DDevice9", pShader );
		return E_FAIL;
	}
	STDMETHOD(GetPixelShader)( IDirect3DPixelShader9** ppShader ) override
	{
		D3D9_STUB( "IDirect3DDevice9", ppShader );
		return E_FAIL;
	}
	STDMETHOD(SetPixelShaderConstantF)( UINT StartRegister, const float* pConstantData, UINT Vector4fCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, Vector4fCount );
		return E_FAIL;
	}
	STDMETHOD(GetPixelShaderConstantF)( UINT StartRegister, float* pConstantData, UINT Vector4fCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, Vector4fCount );
		return E_FAIL;
	}
	STDMETHOD(SetPixelShaderConstantI)( UINT StartRegister, const int* pConstantData, UINT Vector4iCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, Vector4iCount );
		return E_FAIL;
	}
	STDMETHOD(GetPixelShaderConstantI)( UINT StartRegister, int* pConstantData, UINT Vector4iCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, Vector4iCount );
		return E_FAIL;
	}
	STDMETHOD(SetPixelShaderConstantB)( UINT StartRegister, const BOOL* pConstantData, UINT BoolCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, BoolCount );
		return E_FAIL;
	}
	STDMETHOD(GetPixelShaderConstantB)( UINT StartRegister, BOOL* pConstantData, UINT BoolCount ) override
	{
		D3D9_STUB( "IDirect3DDevice9", StartRegister, pConstantData, BoolCount );
		return E_FAIL;
	}
	STDMETHOD(DrawRectPatch)( UINT Handle, const float* pNumSegs, const D3DRECTPATCH_INFO* pRectPatchInfo ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Handle, pNumSegs, pRectPatchInfo );
		return E_FAIL;
	}
	STDMETHOD(DrawTriPatch)( UINT Handle, const float* pNumSegs, const D3DTRIPATCH_INFO* pTriPatchInfo ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Handle, pNumSegs, pTriPatchInfo );
		return E_FAIL;
	}
	STDMETHOD(DeletePatch)( UINT Handle ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Handle );
		return E_FAIL;
	}
	STDMETHOD(CreateQuery)( D3DQUERYTYPE Type, IDirect3DQuery9** ppQuery ) override
	{
		D3D9_STUB( "IDirect3DDevice9", Type, ppQuery );
		return E_FAIL;
	}
	STDMETHOD(SetConvolutionMonoKernel)( UINT width, UINT height, float* rows, float* columns ) override
	{
		D3D9_STUB( "IDirect3DDevice9Ex", width, height, rows, columns );
		return E_FAIL;
	}
	STDMETHOD(ComposeRects)( IDirect3DSurface9* pSrc, IDirect3DSurface9* pDst, IDirect3DVertexBuffer9* pSrcRectDescs, UINT NumRects, IDirect3DVertexBuffer9* pDstRectDescs, D3DCOMPOSERECTSOP Operation, int Xoffset, int Yoffset ) override
	{
		D3D9_STUB( "IDirect3DDevice9Ex", pSrc, pDst, pSrcRectDescs, NumRects, pDstRectDescs, Operation, Xoffset, Yoffset );
		return E_FAIL;
	}
	STDMETHOD(PresentEx)( const RECT* pSourceRect, const RECT* pDestRect, HWND hDestWindowOverride, const RGNDATA* pDirtyRegion, DWORD dwFlags ) override
	{
		D3D9_STUB( "IDirect3DDevice9Ex", pSourceRect, pDestRect, hDestWindowOverride, pDirtyRegion, dwFlags );
		return E_FAIL;
	}
	STDMETHOD(GetGPUThreadPriority)( INT* pPriority ) override
	{
		D3D9_STUB( "IDirect3DDevice9Ex", pPriority );
		return E_FAIL;
	}
	STDMETHOD(SetGPUThreadPriority)( INT Priority ) override
	{
		D3D9_STUB( "IDirect3DDevice9Ex", Priority );
		return E_FAIL;
	}
	STDMETHOD(WaitForVBlank)( UINT iSwapChain ) override
	{
		D3D9_STUB( "IDirect3DDevice9Ex", iSwapChain );
		return E_FAIL;
	}
	STDMETHOD(CheckResourceResidency)( IDirect3DResource9** pResourceArray, UINT32 NumResources ) override
	{
		D3D9_STUB( "IDirect3DDevice9Ex", pResourceArray, NumResources );
		return E_FAIL;
	}
	STDMETHOD(SetMaximumFrameLatency)( UINT MaxLatency ) override
	{
		D3D9_STUB( "IDirect3DDevice9Ex", MaxLatency );
		return E_FAIL;
	}
	STDMETHOD(GetMaximumFrameLatency)( UINT* pMaxLatency ) override
	{
		D3D9_STUB( "IDirect3DDevice9Ex", pMaxLatency );
		return E_FAIL;
	}
	STDMETHOD(CheckDeviceState)( HWND hDestinationWindow ) override
	{
		D3D9_STUB( "IDirect3DDevice9Ex", hDestinationWindow );
		return E_FAIL;
	}
	STDMETHOD(CreateRenderTargetEx)( UINT Width, UINT Height, D3DFORMAT Format, D3DMULTISAMPLE_TYPE MultiSample, DWORD MultisampleQuality, BOOL Lockable, IDirect3DSurface9** ppSurface, HANDLE* pSharedHandle, DWORD Usage ) override
	{
		D3D9_STUB( "IDirect3DDevice9Ex", Width, Height, Format, MultiSample, MultisampleQuality, Lockable, ppSurface, pSharedHandle, Usage );
		return E_FAIL;
	}
	STDMETHOD(CreateOffscreenPlainSurfaceEx)( UINT Width, UINT Height, D3DFORMAT Format, D3DPOOL Pool, IDirect3DSurface9** ppSurface, HANDLE* pSharedHandle, DWORD Usage ) override
	{
		D3D9_STUB( "IDirect3DDevice9Ex", Width, Height, Format, Pool, ppSurface, pSharedHandle, Usage );
		return E_FAIL;
	}
	STDMETHOD(CreateDepthStencilSurfaceEx)( UINT Width, UINT Height, D3DFORMAT Format, D3DMULTISAMPLE_TYPE MultiSample, DWORD MultisampleQuality, BOOL Discard, IDirect3DSurface9** ppSurface, HANDLE* pSharedHandle, DWORD Usage ) override
	{
		D3D9_STUB( "IDirect3DDevice9Ex", Width, Height, Format, MultiSample, MultisampleQuality, Discard, ppSurface, pSharedHandle, Usage );
		return E_FAIL;
	}
	STDMETHOD(ResetEx)( D3DPRESENT_PARAMETERS* pPresentationParameters, D3DDISPLAYMODEEX * pFullscreenDisplayMode ) override
	{
		D3D9_STUB( "IDirect3DDevice9Ex", pPresentationParameters, pFullscreenDisplayMode );
		return E_FAIL;
	}
	STDMETHOD(GetDisplayModeEx)( UINT iSwapChain, D3DDISPLAYMODEEX* pMode, D3DDISPLAYROTATION* pRotation ) override
	{
		D3D9_STUB( "IDirect3DDevice9Ex", iSwapChain, pMode, pRotation );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DStateBlock9Stub : public IDirect3DStateBlock9
{
public:
	virtual ~CDirect3DStateBlock9Stub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DStateBlock9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DStateBlock9 *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DStateBlock9", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(GetDevice)( IDirect3DDevice9** ppDevice ) override
	{
		D3D9_STUB( "IDirect3DStateBlock9", ppDevice );
		return E_FAIL;
	}
	STDMETHOD(Capture)() override
	{
		D3D9_STUB0( "IDirect3DStateBlock9" );
		return E_FAIL;
	}
	STDMETHOD(Apply)() override
	{
		D3D9_STUB0( "IDirect3DStateBlock9" );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DSwapChain9Stub : public IDirect3DSwapChain9
{
public:
	virtual ~CDirect3DSwapChain9Stub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DSwapChain9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DSwapChain9 *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DSwapChain9", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(Present)( const RECT* pSourceRect, const RECT* pDestRect, HWND hDestWindowOverride, const RGNDATA* pDirtyRegion, DWORD dwFlags ) override
	{
		D3D9_STUB( "IDirect3DSwapChain9", pSourceRect, pDestRect, hDestWindowOverride, pDirtyRegion, dwFlags );
		return E_FAIL;
	}
	STDMETHOD(GetFrontBufferData)( IDirect3DSurface9* pDestSurface ) override
	{
		D3D9_STUB( "IDirect3DSwapChain9", pDestSurface );
		return E_FAIL;
	}
	STDMETHOD(GetBackBuffer)( UINT iBackBuffer, D3DBACKBUFFER_TYPE Type, IDirect3DSurface9** ppBackBuffer ) override
	{
		D3D9_STUB( "IDirect3DSwapChain9", iBackBuffer, Type, ppBackBuffer );
		return E_FAIL;
	}
	STDMETHOD(GetRasterStatus)( D3DRASTER_STATUS* pRasterStatus ) override
	{
		D3D9_STUB( "IDirect3DSwapChain9", pRasterStatus );
		return E_FAIL;
	}
	STDMETHOD(GetDisplayMode)( D3DDISPLAYMODE* pMode ) override
	{
		D3D9_STUB( "IDirect3DSwapChain9", pMode );
		return E_FAIL;
	}
	STDMETHOD(GetDevice)( IDirect3DDevice9** ppDevice ) override
	{
		D3D9_STUB( "IDirect3DSwapChain9", ppDevice );
		return E_FAIL;
	}
	STDMETHOD(GetPresentParameters)( D3DPRESENT_PARAMETERS* pPresentationParameters ) override
	{
		D3D9_STUB( "IDirect3DSwapChain9", pPresentationParameters );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DSwapChain9ExStub : public IDirect3DSwapChain9Ex
{
public:
	virtual ~CDirect3DSwapChain9ExStub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DSwapChain9Ex || riid == IID_IDirect3DSwapChain9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DSwapChain9Ex *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DSwapChain9Ex", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(Present)( const RECT* pSourceRect, const RECT* pDestRect, HWND hDestWindowOverride, const RGNDATA* pDirtyRegion, DWORD dwFlags ) override
	{
		D3D9_STUB( "IDirect3DSwapChain9", pSourceRect, pDestRect, hDestWindowOverride, pDirtyRegion, dwFlags );
		return E_FAIL;
	}
	STDMETHOD(GetFrontBufferData)( IDirect3DSurface9* pDestSurface ) override
	{
		D3D9_STUB( "IDirect3DSwapChain9", pDestSurface );
		return E_FAIL;
	}
	STDMETHOD(GetBackBuffer)( UINT iBackBuffer, D3DBACKBUFFER_TYPE Type, IDirect3DSurface9** ppBackBuffer ) override
	{
		D3D9_STUB( "IDirect3DSwapChain9", iBackBuffer, Type, ppBackBuffer );
		return E_FAIL;
	}
	STDMETHOD(GetRasterStatus)( D3DRASTER_STATUS* pRasterStatus ) override
	{
		D3D9_STUB( "IDirect3DSwapChain9", pRasterStatus );
		return E_FAIL;
	}
	STDMETHOD(GetDisplayMode)( D3DDISPLAYMODE* pMode ) override
	{
		D3D9_STUB( "IDirect3DSwapChain9", pMode );
		return E_FAIL;
	}
	STDMETHOD(GetDevice)( IDirect3DDevice9** ppDevice ) override
	{
		D3D9_STUB( "IDirect3DSwapChain9", ppDevice );
		return E_FAIL;
	}
	STDMETHOD(GetPresentParameters)( D3DPRESENT_PARAMETERS* pPresentationParameters ) override
	{
		D3D9_STUB( "IDirect3DSwapChain9", pPresentationParameters );
		return E_FAIL;
	}
	STDMETHOD(GetLastPresentCount)( UINT* pLastPresentCount ) override
	{
		D3D9_STUB( "IDirect3DSwapChain9Ex", pLastPresentCount );
		return E_FAIL;
	}
	STDMETHOD(GetPresentStats)( D3DPRESENTSTATS* pPresentationStatistics ) override
	{
		D3D9_STUB( "IDirect3DSwapChain9Ex", pPresentationStatistics );
		return E_FAIL;
	}
	STDMETHOD(GetDisplayModeEx)( D3DDISPLAYMODEEX* pMode, D3DDISPLAYROTATION* pRotation ) override
	{
		D3D9_STUB( "IDirect3DSwapChain9Ex", pMode, pRotation );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DResource9Stub : public IDirect3DResource9
{
public:
	virtual ~CDirect3DResource9Stub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DResource9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DResource9 *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DResource9", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(GetDevice)( IDirect3DDevice9** ppDevice ) override
	{
		D3D9_STUB( "IDirect3DResource9", ppDevice );
		return E_FAIL;
	}
	STDMETHOD(SetPrivateData)( REFGUID refguid, const void* pData, DWORD SizeOfData, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid, pData, SizeOfData, Flags );
		return E_FAIL;
	}
	STDMETHOD(GetPrivateData)( REFGUID refguid, void* pData, DWORD* pSizeOfData ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid, pData, pSizeOfData );
		return E_FAIL;
	}
	STDMETHOD(FreePrivateData)( REFGUID refguid ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid );
		return E_FAIL;
	}
	STDMETHOD_(DWORD, SetPriority)( DWORD PriorityNew ) override
	{
		D3D9_STUB( "IDirect3DResource9", PriorityNew );
		return {};
	}
	STDMETHOD_(DWORD, GetPriority)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
		return {};
	}
	STDMETHOD_(void, PreLoad)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
	}
	STDMETHOD_(D3DRESOURCETYPE, GetType)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
		return {};
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DVertexDeclaration9Stub : public IDirect3DVertexDeclaration9
{
public:
	virtual ~CDirect3DVertexDeclaration9Stub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DVertexDeclaration9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DVertexDeclaration9 *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DVertexDeclaration9", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(GetDevice)( IDirect3DDevice9** ppDevice ) override
	{
		D3D9_STUB( "IDirect3DVertexDeclaration9", ppDevice );
		return E_FAIL;
	}
	STDMETHOD(GetDeclaration)( D3DVERTEXELEMENT9* pElement, UINT* pNumElements ) override
	{
		D3D9_STUB( "IDirect3DVertexDeclaration9", pElement, pNumElements );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DVertexShader9Stub : public IDirect3DVertexShader9
{
public:
	virtual ~CDirect3DVertexShader9Stub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DVertexShader9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DVertexShader9 *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DVertexShader9", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(GetDevice)( IDirect3DDevice9** ppDevice ) override
	{
		D3D9_STUB( "IDirect3DVertexShader9", ppDevice );
		return E_FAIL;
	}
	STDMETHOD(GetFunction)( void* arg0, UINT* pSizeOfData ) override
	{
		D3D9_STUB( "IDirect3DVertexShader9", arg0, pSizeOfData );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DPixelShader9Stub : public IDirect3DPixelShader9
{
public:
	virtual ~CDirect3DPixelShader9Stub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DPixelShader9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DPixelShader9 *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DPixelShader9", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(GetDevice)( IDirect3DDevice9** ppDevice ) override
	{
		D3D9_STUB( "IDirect3DPixelShader9", ppDevice );
		return E_FAIL;
	}
	STDMETHOD(GetFunction)( void* arg0, UINT* pSizeOfData ) override
	{
		D3D9_STUB( "IDirect3DPixelShader9", arg0, pSizeOfData );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DBaseTexture9Stub : public IDirect3DBaseTexture9
{
public:
	virtual ~CDirect3DBaseTexture9Stub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DBaseTexture9 || riid == IID_IDirect3DResource9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DBaseTexture9 *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DBaseTexture9", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(GetDevice)( IDirect3DDevice9** ppDevice ) override
	{
		D3D9_STUB( "IDirect3DResource9", ppDevice );
		return E_FAIL;
	}
	STDMETHOD(SetPrivateData)( REFGUID refguid, const void* pData, DWORD SizeOfData, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid, pData, SizeOfData, Flags );
		return E_FAIL;
	}
	STDMETHOD(GetPrivateData)( REFGUID refguid, void* pData, DWORD* pSizeOfData ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid, pData, pSizeOfData );
		return E_FAIL;
	}
	STDMETHOD(FreePrivateData)( REFGUID refguid ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid );
		return E_FAIL;
	}
	STDMETHOD_(DWORD, SetPriority)( DWORD PriorityNew ) override
	{
		D3D9_STUB( "IDirect3DResource9", PriorityNew );
		return {};
	}
	STDMETHOD_(DWORD, GetPriority)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
		return {};
	}
	STDMETHOD_(void, PreLoad)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
	}
	STDMETHOD_(D3DRESOURCETYPE, GetType)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
		return {};
	}
	STDMETHOD_(DWORD, SetLOD)( DWORD LODNew ) override
	{
		D3D9_STUB( "IDirect3DBaseTexture9", LODNew );
		return {};
	}
	STDMETHOD_(DWORD, GetLOD)() override
	{
		D3D9_STUB0( "IDirect3DBaseTexture9" );
		return {};
	}
	STDMETHOD_(DWORD, GetLevelCount)() override
	{
		D3D9_STUB0( "IDirect3DBaseTexture9" );
		return {};
	}
	STDMETHOD(SetAutoGenFilterType)( D3DTEXTUREFILTERTYPE FilterType ) override
	{
		D3D9_STUB( "IDirect3DBaseTexture9", FilterType );
		return E_FAIL;
	}
	STDMETHOD_(D3DTEXTUREFILTERTYPE, GetAutoGenFilterType)() override
	{
		D3D9_STUB0( "IDirect3DBaseTexture9" );
		return {};
	}
	STDMETHOD_(void, GenerateMipSubLevels)() override
	{
		D3D9_STUB0( "IDirect3DBaseTexture9" );
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DTexture9Stub : public IDirect3DTexture9
{
public:
	virtual ~CDirect3DTexture9Stub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DTexture9 || riid == IID_IDirect3DBaseTexture9 || riid == IID_IDirect3DResource9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DTexture9 *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DTexture9", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(GetDevice)( IDirect3DDevice9** ppDevice ) override
	{
		D3D9_STUB( "IDirect3DResource9", ppDevice );
		return E_FAIL;
	}
	STDMETHOD(SetPrivateData)( REFGUID refguid, const void* pData, DWORD SizeOfData, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid, pData, SizeOfData, Flags );
		return E_FAIL;
	}
	STDMETHOD(GetPrivateData)( REFGUID refguid, void* pData, DWORD* pSizeOfData ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid, pData, pSizeOfData );
		return E_FAIL;
	}
	STDMETHOD(FreePrivateData)( REFGUID refguid ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid );
		return E_FAIL;
	}
	STDMETHOD_(DWORD, SetPriority)( DWORD PriorityNew ) override
	{
		D3D9_STUB( "IDirect3DResource9", PriorityNew );
		return {};
	}
	STDMETHOD_(DWORD, GetPriority)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
		return {};
	}
	STDMETHOD_(void, PreLoad)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
	}
	STDMETHOD_(D3DRESOURCETYPE, GetType)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
		return {};
	}
	STDMETHOD_(DWORD, SetLOD)( DWORD LODNew ) override
	{
		D3D9_STUB( "IDirect3DBaseTexture9", LODNew );
		return {};
	}
	STDMETHOD_(DWORD, GetLOD)() override
	{
		D3D9_STUB0( "IDirect3DBaseTexture9" );
		return {};
	}
	STDMETHOD_(DWORD, GetLevelCount)() override
	{
		D3D9_STUB0( "IDirect3DBaseTexture9" );
		return {};
	}
	STDMETHOD(SetAutoGenFilterType)( D3DTEXTUREFILTERTYPE FilterType ) override
	{
		D3D9_STUB( "IDirect3DBaseTexture9", FilterType );
		return E_FAIL;
	}
	STDMETHOD_(D3DTEXTUREFILTERTYPE, GetAutoGenFilterType)() override
	{
		D3D9_STUB0( "IDirect3DBaseTexture9" );
		return {};
	}
	STDMETHOD_(void, GenerateMipSubLevels)() override
	{
		D3D9_STUB0( "IDirect3DBaseTexture9" );
	}
	STDMETHOD(GetLevelDesc)( UINT Level, D3DSURFACE_DESC * pDesc ) override
	{
		D3D9_STUB( "IDirect3DTexture9", Level, pDesc );
		return E_FAIL;
	}
	STDMETHOD(GetSurfaceLevel)( UINT Level, IDirect3DSurface9** ppSurfaceLevel ) override
	{
		D3D9_STUB( "IDirect3DTexture9", Level, ppSurfaceLevel );
		return E_FAIL;
	}
	STDMETHOD(LockRect)( UINT Level, D3DLOCKED_RECT* pLockedRect, const RECT* pRect, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DTexture9", Level, pLockedRect, pRect, Flags );
		return E_FAIL;
	}
	STDMETHOD(UnlockRect)( UINT Level ) override
	{
		D3D9_STUB( "IDirect3DTexture9", Level );
		return E_FAIL;
	}
	STDMETHOD(AddDirtyRect)( const RECT* pDirtyRect ) override
	{
		D3D9_STUB( "IDirect3DTexture9", pDirtyRect );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DVolumeTexture9Stub : public IDirect3DVolumeTexture9
{
public:
	virtual ~CDirect3DVolumeTexture9Stub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DVolumeTexture9 || riid == IID_IDirect3DBaseTexture9 || riid == IID_IDirect3DResource9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DVolumeTexture9 *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DVolumeTexture9", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(GetDevice)( IDirect3DDevice9** ppDevice ) override
	{
		D3D9_STUB( "IDirect3DResource9", ppDevice );
		return E_FAIL;
	}
	STDMETHOD(SetPrivateData)( REFGUID refguid, const void* pData, DWORD SizeOfData, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid, pData, SizeOfData, Flags );
		return E_FAIL;
	}
	STDMETHOD(GetPrivateData)( REFGUID refguid, void* pData, DWORD* pSizeOfData ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid, pData, pSizeOfData );
		return E_FAIL;
	}
	STDMETHOD(FreePrivateData)( REFGUID refguid ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid );
		return E_FAIL;
	}
	STDMETHOD_(DWORD, SetPriority)( DWORD PriorityNew ) override
	{
		D3D9_STUB( "IDirect3DResource9", PriorityNew );
		return {};
	}
	STDMETHOD_(DWORD, GetPriority)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
		return {};
	}
	STDMETHOD_(void, PreLoad)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
	}
	STDMETHOD_(D3DRESOURCETYPE, GetType)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
		return {};
	}
	STDMETHOD_(DWORD, SetLOD)( DWORD LODNew ) override
	{
		D3D9_STUB( "IDirect3DBaseTexture9", LODNew );
		return {};
	}
	STDMETHOD_(DWORD, GetLOD)() override
	{
		D3D9_STUB0( "IDirect3DBaseTexture9" );
		return {};
	}
	STDMETHOD_(DWORD, GetLevelCount)() override
	{
		D3D9_STUB0( "IDirect3DBaseTexture9" );
		return {};
	}
	STDMETHOD(SetAutoGenFilterType)( D3DTEXTUREFILTERTYPE FilterType ) override
	{
		D3D9_STUB( "IDirect3DBaseTexture9", FilterType );
		return E_FAIL;
	}
	STDMETHOD_(D3DTEXTUREFILTERTYPE, GetAutoGenFilterType)() override
	{
		D3D9_STUB0( "IDirect3DBaseTexture9" );
		return {};
	}
	STDMETHOD_(void, GenerateMipSubLevels)() override
	{
		D3D9_STUB0( "IDirect3DBaseTexture9" );
	}
	STDMETHOD(GetLevelDesc)( UINT Level, D3DVOLUME_DESC * pDesc ) override
	{
		D3D9_STUB( "IDirect3DVolumeTexture9", Level, pDesc );
		return E_FAIL;
	}
	STDMETHOD(GetVolumeLevel)( UINT Level, IDirect3DVolume9** ppVolumeLevel ) override
	{
		D3D9_STUB( "IDirect3DVolumeTexture9", Level, ppVolumeLevel );
		return E_FAIL;
	}
	STDMETHOD(LockBox)( UINT Level, D3DLOCKED_BOX* pLockedVolume, const D3DBOX* pBox, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DVolumeTexture9", Level, pLockedVolume, pBox, Flags );
		return E_FAIL;
	}
	STDMETHOD(UnlockBox)( UINT Level ) override
	{
		D3D9_STUB( "IDirect3DVolumeTexture9", Level );
		return E_FAIL;
	}
	STDMETHOD(AddDirtyBox)( const D3DBOX* pDirtyBox ) override
	{
		D3D9_STUB( "IDirect3DVolumeTexture9", pDirtyBox );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DCubeTexture9Stub : public IDirect3DCubeTexture9
{
public:
	virtual ~CDirect3DCubeTexture9Stub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DCubeTexture9 || riid == IID_IDirect3DBaseTexture9 || riid == IID_IDirect3DResource9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DCubeTexture9 *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DCubeTexture9", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(GetDevice)( IDirect3DDevice9** ppDevice ) override
	{
		D3D9_STUB( "IDirect3DResource9", ppDevice );
		return E_FAIL;
	}
	STDMETHOD(SetPrivateData)( REFGUID refguid, const void* pData, DWORD SizeOfData, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid, pData, SizeOfData, Flags );
		return E_FAIL;
	}
	STDMETHOD(GetPrivateData)( REFGUID refguid, void* pData, DWORD* pSizeOfData ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid, pData, pSizeOfData );
		return E_FAIL;
	}
	STDMETHOD(FreePrivateData)( REFGUID refguid ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid );
		return E_FAIL;
	}
	STDMETHOD_(DWORD, SetPriority)( DWORD PriorityNew ) override
	{
		D3D9_STUB( "IDirect3DResource9", PriorityNew );
		return {};
	}
	STDMETHOD_(DWORD, GetPriority)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
		return {};
	}
	STDMETHOD_(void, PreLoad)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
	}
	STDMETHOD_(D3DRESOURCETYPE, GetType)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
		return {};
	}
	STDMETHOD_(DWORD, SetLOD)( DWORD LODNew ) override
	{
		D3D9_STUB( "IDirect3DBaseTexture9", LODNew );
		return {};
	}
	STDMETHOD_(DWORD, GetLOD)() override
	{
		D3D9_STUB0( "IDirect3DBaseTexture9" );
		return {};
	}
	STDMETHOD_(DWORD, GetLevelCount)() override
	{
		D3D9_STUB0( "IDirect3DBaseTexture9" );
		return {};
	}
	STDMETHOD(SetAutoGenFilterType)( D3DTEXTUREFILTERTYPE FilterType ) override
	{
		D3D9_STUB( "IDirect3DBaseTexture9", FilterType );
		return E_FAIL;
	}
	STDMETHOD_(D3DTEXTUREFILTERTYPE, GetAutoGenFilterType)() override
	{
		D3D9_STUB0( "IDirect3DBaseTexture9" );
		return {};
	}
	STDMETHOD_(void, GenerateMipSubLevels)() override
	{
		D3D9_STUB0( "IDirect3DBaseTexture9" );
	}
	STDMETHOD(GetLevelDesc)( UINT Level, D3DSURFACE_DESC * pDesc ) override
	{
		D3D9_STUB( "IDirect3DCubeTexture9", Level, pDesc );
		return E_FAIL;
	}
	STDMETHOD(GetCubeMapSurface)( D3DCUBEMAP_FACES FaceType, UINT Level, IDirect3DSurface9** ppCubeMapSurface ) override
	{
		D3D9_STUB( "IDirect3DCubeTexture9", FaceType, Level, ppCubeMapSurface );
		return E_FAIL;
	}
	STDMETHOD(LockRect)( D3DCUBEMAP_FACES FaceType, UINT Level, D3DLOCKED_RECT* pLockedRect, const RECT* pRect, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DCubeTexture9", FaceType, Level, pLockedRect, pRect, Flags );
		return E_FAIL;
	}
	STDMETHOD(UnlockRect)( D3DCUBEMAP_FACES FaceType, UINT Level ) override
	{
		D3D9_STUB( "IDirect3DCubeTexture9", FaceType, Level );
		return E_FAIL;
	}
	STDMETHOD(AddDirtyRect)( D3DCUBEMAP_FACES FaceType, const RECT* pDirtyRect ) override
	{
		D3D9_STUB( "IDirect3DCubeTexture9", FaceType, pDirtyRect );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DVertexBuffer9Stub : public IDirect3DVertexBuffer9
{
public:
	virtual ~CDirect3DVertexBuffer9Stub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DVertexBuffer9 || riid == IID_IDirect3DResource9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DVertexBuffer9 *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DVertexBuffer9", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(GetDevice)( IDirect3DDevice9** ppDevice ) override
	{
		D3D9_STUB( "IDirect3DResource9", ppDevice );
		return E_FAIL;
	}
	STDMETHOD(SetPrivateData)( REFGUID refguid, const void* pData, DWORD SizeOfData, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid, pData, SizeOfData, Flags );
		return E_FAIL;
	}
	STDMETHOD(GetPrivateData)( REFGUID refguid, void* pData, DWORD* pSizeOfData ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid, pData, pSizeOfData );
		return E_FAIL;
	}
	STDMETHOD(FreePrivateData)( REFGUID refguid ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid );
		return E_FAIL;
	}
	STDMETHOD_(DWORD, SetPriority)( DWORD PriorityNew ) override
	{
		D3D9_STUB( "IDirect3DResource9", PriorityNew );
		return {};
	}
	STDMETHOD_(DWORD, GetPriority)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
		return {};
	}
	STDMETHOD_(void, PreLoad)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
	}
	STDMETHOD_(D3DRESOURCETYPE, GetType)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
		return {};
	}
	STDMETHOD(Lock)( UINT OffsetToLock, UINT SizeToLock, void** ppbData, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DVertexBuffer9", OffsetToLock, SizeToLock, ppbData, Flags );
		return E_FAIL;
	}
	STDMETHOD(Unlock)() override
	{
		D3D9_STUB0( "IDirect3DVertexBuffer9" );
		return E_FAIL;
	}
	STDMETHOD(GetDesc)( D3DVERTEXBUFFER_DESC * pDesc ) override
	{
		D3D9_STUB( "IDirect3DVertexBuffer9", pDesc );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DIndexBuffer9Stub : public IDirect3DIndexBuffer9
{
public:
	virtual ~CDirect3DIndexBuffer9Stub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DIndexBuffer9 || riid == IID_IDirect3DResource9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DIndexBuffer9 *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DIndexBuffer9", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(GetDevice)( IDirect3DDevice9** ppDevice ) override
	{
		D3D9_STUB( "IDirect3DResource9", ppDevice );
		return E_FAIL;
	}
	STDMETHOD(SetPrivateData)( REFGUID refguid, const void* pData, DWORD SizeOfData, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid, pData, SizeOfData, Flags );
		return E_FAIL;
	}
	STDMETHOD(GetPrivateData)( REFGUID refguid, void* pData, DWORD* pSizeOfData ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid, pData, pSizeOfData );
		return E_FAIL;
	}
	STDMETHOD(FreePrivateData)( REFGUID refguid ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid );
		return E_FAIL;
	}
	STDMETHOD_(DWORD, SetPriority)( DWORD PriorityNew ) override
	{
		D3D9_STUB( "IDirect3DResource9", PriorityNew );
		return {};
	}
	STDMETHOD_(DWORD, GetPriority)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
		return {};
	}
	STDMETHOD_(void, PreLoad)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
	}
	STDMETHOD_(D3DRESOURCETYPE, GetType)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
		return {};
	}
	STDMETHOD(Lock)( UINT OffsetToLock, UINT SizeToLock, void** ppbData, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DIndexBuffer9", OffsetToLock, SizeToLock, ppbData, Flags );
		return E_FAIL;
	}
	STDMETHOD(Unlock)() override
	{
		D3D9_STUB0( "IDirect3DIndexBuffer9" );
		return E_FAIL;
	}
	STDMETHOD(GetDesc)( D3DINDEXBUFFER_DESC * pDesc ) override
	{
		D3D9_STUB( "IDirect3DIndexBuffer9", pDesc );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DSurface9Stub : public IDirect3DSurface9
{
public:
	virtual ~CDirect3DSurface9Stub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DSurface9 || riid == IID_IDirect3DResource9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DSurface9 *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DSurface9", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(GetDevice)( IDirect3DDevice9** ppDevice ) override
	{
		D3D9_STUB( "IDirect3DResource9", ppDevice );
		return E_FAIL;
	}
	STDMETHOD(SetPrivateData)( REFGUID refguid, const void* pData, DWORD SizeOfData, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid, pData, SizeOfData, Flags );
		return E_FAIL;
	}
	STDMETHOD(GetPrivateData)( REFGUID refguid, void* pData, DWORD* pSizeOfData ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid, pData, pSizeOfData );
		return E_FAIL;
	}
	STDMETHOD(FreePrivateData)( REFGUID refguid ) override
	{
		D3D9_STUB( "IDirect3DResource9", refguid );
		return E_FAIL;
	}
	STDMETHOD_(DWORD, SetPriority)( DWORD PriorityNew ) override
	{
		D3D9_STUB( "IDirect3DResource9", PriorityNew );
		return {};
	}
	STDMETHOD_(DWORD, GetPriority)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
		return {};
	}
	STDMETHOD_(void, PreLoad)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
	}
	STDMETHOD_(D3DRESOURCETYPE, GetType)() override
	{
		D3D9_STUB0( "IDirect3DResource9" );
		return {};
	}
	STDMETHOD(GetContainer)( REFIID riid, void** ppContainer ) override
	{
		D3D9_STUB( "IDirect3DSurface9", riid, ppContainer );
		return E_FAIL;
	}
	STDMETHOD(GetDesc)( D3DSURFACE_DESC * pDesc ) override
	{
		D3D9_STUB( "IDirect3DSurface9", pDesc );
		return E_FAIL;
	}
	STDMETHOD(LockRect)( D3DLOCKED_RECT* pLockedRect, const RECT* pRect, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DSurface9", pLockedRect, pRect, Flags );
		return E_FAIL;
	}
	STDMETHOD(UnlockRect)() override
	{
		D3D9_STUB0( "IDirect3DSurface9" );
		return E_FAIL;
	}
	STDMETHOD(GetDC)( HDC * phdc ) override
	{
		D3D9_STUB( "IDirect3DSurface9", phdc );
		return E_FAIL;
	}
	STDMETHOD(ReleaseDC)( HDC hdc ) override
	{
		D3D9_STUB( "IDirect3DSurface9", hdc );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DVolume9Stub : public IDirect3DVolume9
{
public:
	virtual ~CDirect3DVolume9Stub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DVolume9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DVolume9 *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DVolume9", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(GetDevice)( IDirect3DDevice9** ppDevice ) override
	{
		D3D9_STUB( "IDirect3DVolume9", ppDevice );
		return E_FAIL;
	}
	STDMETHOD(SetPrivateData)( REFGUID refguid, const void* pData, DWORD SizeOfData, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DVolume9", refguid, pData, SizeOfData, Flags );
		return E_FAIL;
	}
	STDMETHOD(GetPrivateData)( REFGUID refguid, void* pData, DWORD* pSizeOfData ) override
	{
		D3D9_STUB( "IDirect3DVolume9", refguid, pData, pSizeOfData );
		return E_FAIL;
	}
	STDMETHOD(FreePrivateData)( REFGUID refguid ) override
	{
		D3D9_STUB( "IDirect3DVolume9", refguid );
		return E_FAIL;
	}
	STDMETHOD(GetContainer)( REFIID riid, void** ppContainer ) override
	{
		D3D9_STUB( "IDirect3DVolume9", riid, ppContainer );
		return E_FAIL;
	}
	STDMETHOD(GetDesc)( D3DVOLUME_DESC * pDesc ) override
	{
		D3D9_STUB( "IDirect3DVolume9", pDesc );
		return E_FAIL;
	}
	STDMETHOD(LockBox)( D3DLOCKED_BOX * pLockedVolume, const D3DBOX* pBox, DWORD Flags ) override
	{
		D3D9_STUB( "IDirect3DVolume9", pLockedVolume, pBox, Flags );
		return E_FAIL;
	}
	STDMETHOD(UnlockBox)() override
	{
		D3D9_STUB0( "IDirect3DVolume9" );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

class CDirect3DQuery9Stub : public IDirect3DQuery9
{
public:
	virtual ~CDirect3DQuery9Stub() {}

	// IUnknown, for real.
	STDMETHOD(QueryInterface)( REFIID riid, void **ppvObj ) override
	{
		if ( ppvObj == nullptr )
		{
			return E_POINTER;
		}
		if ( riid == IID_IDirect3DQuery9 || riid == IID_IUnknown )
		{
			*ppvObj = static_cast<IDirect3DQuery9 *>( this );
			AddRef();
			return S_OK;
		}
		*ppvObj = nullptr;
		D3D9_STUB( "IDirect3DQuery9", riid );
		return E_NOINTERFACE;
	}
	STDMETHOD_(ULONG, AddRef)() override
	{
		return ++nRefs;
	}
	STDMETHOD_(ULONG, Release)() override
	{
		const ULONG nLeft = --nRefs;
		if ( nLeft == 0 )
		{
			delete this;
		}
		return nLeft;
	}
	STDMETHOD(GetDevice)( IDirect3DDevice9** ppDevice ) override
	{
		D3D9_STUB( "IDirect3DQuery9", ppDevice );
		return E_FAIL;
	}
	STDMETHOD_(D3DQUERYTYPE, GetType)() override
	{
		D3D9_STUB0( "IDirect3DQuery9" );
		return {};
	}
	STDMETHOD_(DWORD, GetDataSize)() override
	{
		D3D9_STUB0( "IDirect3DQuery9" );
		return {};
	}
	STDMETHOD(Issue)( DWORD dwIssueFlags ) override
	{
		D3D9_STUB( "IDirect3DQuery9", dwIssueFlags );
		return E_FAIL;
	}
	STDMETHOD(GetData)( void* pData, DWORD dwSize, DWORD dwGetDataFlags ) override
	{
		D3D9_STUB( "IDirect3DQuery9", pData, dwSize, dwGetDataFlags );
		return E_FAIL;
	}

protected:
	std::atomic<ULONG> nRefs{ 1 };
};

}  // namespace ND3D9Stub
