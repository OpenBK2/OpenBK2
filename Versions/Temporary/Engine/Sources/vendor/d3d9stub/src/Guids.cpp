// The IID_* constants the generated QueryInterface bodies compare against.
// d3d9.h declares them with DEFINE_GUID, which only defines them where
// initguid.h came first -- here, once, rather than linking dxguid.lib.

#include <windows.h>
#include <initguid.h>
#include <d3d9.h>
