#include "stdafx.h"

#include "MountedGun.h"
#include "SerializeOwner.h"
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
int CCommonMountedGun::operator&( IBinSaver &saver )
{
	saver.Add( 1, &pBuildingStats );
	SerializeOwner( 2, &pBuilding, &saver );
	saver.Add( 3, &pTurret );
	saver.Add( 4, &nSlot );

	return 0;
}
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

